---
phase: 02-telegram-ledger-assistant
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/telegram/media.ts
  - src/telegram/openrouter.ts
  - src/telegram/verify.ts
  - src/telegram/commands.ts
  - src/worker.ts
  - package.json
  - tests/telegram/media.test.ts
  - tests/telegram/verify.test.ts
autonomous: true
requirements:
  - 2-R2
  - 2-R5
estimate:
  tokens: 62000
  raw_tokens: 31000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "Admin can send a photo (or document image) to the bot with optional caption and get back FOUND / MULTIPLE / NOT_FOUND with near-matches handling via deterministic SELECT on transactions (amount exact + date exact + sender ILIKE), plus 'Non-Zenith receipt — searching Zenith ledger 999****999' note when bank not Zenith (per D-01, D-02, D-05, D-06)"
    - "Admin can send a PDF document: text PDFs are parsed locally via pdf-parse first; only if extracted text <40 chars does OpenRouter file_data with file-parser fallback handle it (per D-03)"
    - "Admin can send free-form text like '100k 2026-09-09 SAMPLE SENDER' and get a match via loose local regex (100k→100000, DD/MM or ISO date, sender via extractSender) without calling OpenRouter; AI text parse only when regex missing amount or date (per D-02 'when absolutely necessary')"
    - "Same image/PDF bytes sent twice within 24h returns 'Already verified: amount NGN from SENDER on date' from SHA256 content_hash dedup Map without re-calling OpenRouter or DB (per D-04)"
    - "Temp files are written to os.tmpdir as tg-verify-<sha256>.<ext> and auto-unlinked after 24h (unref) plus boot sweep of stale files; 20MB limit enforced and missing file_path returns friendly 'File too large / try compressed image' reply (per D-13)"
    - "OpenRouter vision (image_url base64) is only invoked when caption/text regex fails and when OPENROUTER_API_KEY is set; otherwise graceful text-only fallback (per D-01, RESEARCH A4)"
  artifacts:
    - src/telegram/media.ts exporting getLargestPhotoId, downloadTelegramFile (getFile POST then GET /file/bot<TOKEN>/<path>), tmpWriteWithHash (SHA256 + os.tmpdir + 24h unlink), cleanupStaleTmp
    - src/telegram/openrouter.ts exporting openRouterVision, openRouterPdf, openRouterTextParse (fetch POST https://openrouter.ai/api/v1/chat/completions with image_url/file_data base64, response_format json_object, temperature 0)
    - src/telegram/verify.ts exporting handleVerify, deterministicMatch, nearMatch, verifyContentHash dedup Map and extractFreeForm helper
    - src/telegram/commands.ts extended with /verify dispatch for photo/document/text/caption plus 5/60s rate limit
  key_links:
    - "Telegram update photo[] -> getLargestPhotoId (largest file_size) -> downloadTelegramFile(file_id, token) POST getFile -> GET /file/bot<TOKEN>/<file_path> -> buffer -> tmpWriteWithHash SHA256 -> dedup Map check -> local caption regex -> OpenRouter vision only on miss -> deterministic SELECT"
    - "document application/pdf -> download -> pdf-parse text length check >=40 -> local parse -> else openRouterPdf file_data + plugins file-parser mistral-ocr -> JSON {amount,currency,date,sender}"
    - "free-form text -> parseFreeForm local regex (k→*1000, currency default NGN, date DD/MM or ISO via date-fns) -> if null ask to clarify else deterministic SELECT; only then openRouterTextParse fallback"
    - "deterministic SELECT amount::numeric exact + transaction_date::date exact + sender_name ILIKE -> FOUND(1)/MULTIPLE(>1)/NOT_FOUND -> near-matches WHERE amount=$1 AND date BETWEEN date-1 AND date+1 AND similarity(sender_name,$2)>0.3 LIMIT 3 GIN"
---

<objective>
Multi-modal verify branch: Telegram 2-step media download with 20MB cap + SHA256 dedup + 24h temp lifecycle, loose free-form text parsing, PDF text-first then OpenRouter vision/file fallback only when necessary, deterministic ledger match with near-matches threshold and non-Zenith note.

Purpose: Deliver the core admin verification promise (images, PDFs, free-form text all land on the same transactions SELECT) with cost gates and dedup so repeated verifies don't burn OpenRouter credits.
Output: Working /verify for all three input modes, gated behind /login session and 5/60s rate limit, reusing pg/zod/date-fns + only new dep pdf-parse, on same PORT 8080 worker.
</objective>

<execution_context>
@C:/Users/user/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/user/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@C:/Users/user/Documents/Example Project/.planning/phases/02-telegram-ledger-assistant/02-CONTEXT.md
@C:/Users/user/Documents/Example Project/.planning/phases/02-telegram-ledger-assistant/02-RESEARCH.md
@src/worker.ts
@src/telegram/webhook.ts
@src/telegram/commands.ts
@src/telegram/sendMessage.ts
@src/telegram/session.ts
@src/telegram/balance.ts
@src/telegram/history.ts
@src/zenith/sender.ts
@src/zenith/parser.ts
@src/db/pool.ts
</context>

<tasks>

<task type="auto">
  <name>Telegram 2-step media download + 24h tmp + SHA256 dedup + pdf-parse install</name>
  <files>src/telegram/media.ts, src/telegram/verify.ts, src/worker.ts, package.json</files>
  <read_first>src/telegram/webhook.ts:1-59 (extractChatId, parseCommandText), src/telegram/sendMessage.ts:1-102 (sendTelegramMessage retry 429, escapeHtml), src/telegram/session.ts (isLoggedIn map just created in 02-01), src/telegram/rateLimit.ts:1-30, package.json:18-37 (existing deps, check pdf-parse not yet installed), src/zenith/sender.ts:60-135 (extractSender)</read_first>
  <action>
    Install exactly one new runtime dep per RESEARCH Standard Stack approval: run `npm install pdf-parse@2.4.5` and verify `npm view pdf-parse scripts.postinstall` returns null (no postinstall risk) per Package Legitimacy Audit. Commit package.json + package-lock.json change in this task. Do NOT install openai SDK — use raw fetch for OpenRouter.

    Create src/telegram/media.ts per RESEARCH Pattern 2:
    - export getLargestPhotoId(photo:Array&lt;{file_id:string;file_size?:number}&gt;):string|null sorting by file_size desc pick largest; guard empty.
    - export async downloadTelegramFile(fileId:string, token:string):Promise&lt;{buffer:Buffer;filePath:string;fileSize?:number}&gt; that does POST https://api.telegram.org/bot${token}/getFile {file_id: fileId} JSON, parse {ok,result:{file_path,file_size,description}}, throw friendly "getFile failed: ..." if not ok or missing file_path (covers 20MB too-large case D-??). Then GET https://api.telegram.org/file/bot${token}/${file_path}, check res.ok else throw, Buffer.from(await res.arrayBuffer()), enforce buffer.length &gt;20*1024*1024 throw "File too large — 20MB max via Telegram cloud. Send compressed image or use local Bot API server". Return buffer.
    - export async tmpWriteWithHash(buffer:Buffer, ext:string=".jpg"):Promise&lt;{tmpPath:string;contentHash:string}&gt; compute createHash('sha256').update(buffer).digest('hex'), join(tmpdir(), `tg-verify-${contentHash}${ext}`), await writeFile, setTimeout(()=>unlink(tmpPath).catch(()=>{}), 24*60*60*1000).unref() per D-13, return both. Export cleanupStaleTmp() that readdir(tmpdir()), stat mtime, unlink files matching tg-verify-* older than 24h — called on worker boot.
    - Handle mime-based ext mapping: image/jpeg→.jpg, image/png→.png, image/webp→.webp, application/pdf→.pdf fallback .bin. Never store file_path URL beyond 1h — store file_id + content_hash instead.

    Create src/telegram/verify.ts dedup skeleton: const verifyCache = new Map&lt;string,{result:string; at:number}&gt; where key is contentHash, value cached reply text + at timestamp; export isDuplicate(contentHash)=>check &lt;24h; export cacheVerify(contentHash,replyText). Export helper getTelegramToken():string via getBotToken pattern (TELEGRAM_BOT_TOKEN direct or ALERT_WEBHOOK_URL parse). Export function to build non-Zenith note detection: if extracted sender/bank hint suggests non-Zenith (e.g., caption contains GTB/Access), still search Zenith with transparent note per D-06 — detection is heuristic: if rawText contains GTB|Access|FirstBank etc and not Zenith, add note `Non-Zenith receipt — searching Zenith ledger 999****999`.

    Extend src/worker.ts: on boot after httpServer.listen, call cleanupStaleTmp().catch(()=>{}) sweeping tg-verify-* older than 24h (handles restart lost setTimeout per Pitfall 8). In _resetWorkerStateForTests also clear verifyCache via exported helper.

    Reuse node:crypto, fs/promises, os.tmpdir, path.join, fetch — zero framework deps beyond pdf-parse. Log pino with chatId+contentHash prefix, never full sender_account.
  </action>
  <verify>
    <automated>npm test -- tests/telegram/media.test.ts --reporter=verbose</automated>
  </verify>
  <done>pdf-parse@2.4.5 installed with null postinstall; getLargestPhotoId picks largest file_size; downloadTelegramFile does getFile POST then GET /file/ with 20MB guard and missing file_path friendly error; tmpWriteWithHash writes SHA256-named file to os.tmpdir with 24h unref unlink and boot sweep handles stale files; dedup Map caches 24h without DB.</done>
</task>

<task type="auto">
  <name>Verify extraction: local regex first, OpenRouter vision/PDF only when necessary + deterministic match + near-matches</name>
  <files>src/telegram/openrouter.ts, src/telegram/verify.ts, src/telegram/commands.ts, src/worker.ts</files>
  <read_first>src/zenith/sender.ts:60-135, src/telegram/media.ts (just created), migrations/004_telegram_ledger_indexes.sql (GIN indexes for similarity), src/db/pool.ts:1-41, src/telegram/sendMessage.ts:1-102, .planning/phases/02-telegram-ledger-assistant/02-RESEARCH.md:Patterns 3-4 (deterministic SELECT SQL + near-match similarity>0.3)</read_first>
  <action>
    Create src/telegram/openrouter.ts per RESEARCH Pattern 3:
    - Define constant OPENROUTER_URL = 'https://openrouter.ai/api/v1/chat/completions', headers include Authorization Bearer OPENROUTER_API_KEY, Content-Type json, HTTP-Referer https://example.com, X-Title PaymentVerificationBot.
    - export async openRouterVision(imageBase64:string, mime:string='image/jpeg'):Promise&lt;{amount:number;currency:string;date:string;sender:string}|null&gt; : if !process.env.OPENROUTER_API_KEY return null; build dataUrl `data:${mime};base64,${imageBase64}`, fetch POST with model 'google/gemini-3-flash-preview' (vision) messages [{role:user, content:[{type:text, text:'Extract from this receipt: amount (numeric), currency (NGN/USD/EUR/GBP), date (YYYY-MM-DD), sender name. Return JSON only...'}, {type:image_url, image_url:{url:dataUrl}}]}], response_format:{type:'json_object'}, max_tokens 300, temperature 0; parse JSON from choices[0].message.content try/catch → null; on 429 throw retriable error but caller will reply fallback.
    - export async openRouterPdf(pdfBase64:string):Promise&lt;{...}|null&gt; similar but messages include {type:'file', file:{filename:'receipt.pdf', file_data:`data:application/pdf;base64,${pdfBase64}`}} and plugins [{id:'file-parser', pdf:{engine:'mistral-ocr'}}] per RESEARCH, same JSON parse.
    - export async openRouterTextParse(text:string):Promise&lt;{...}|null&gt; cheap text model 'google/gemma-3-27b-it' with prompt 'Extract amount,currency,date,sender from text "X"; today is Lagos date; return JSON' + response_format json_object; only called when local parse fails.
    - All three return null on missing key or non-200 without throwing to caller (log warn) except 429 which surfaces for rate-limit reply.

    Extend src/telegram/verify.ts:
    - Helper parseFreeForm(text:string):{amount?:number;currency?:string;date?:string;sender?:string}|null per RESEARCH: kMatch `/(\d[\d,]*\.?\d*)\s*k\b/i` → *1000, else /([\d,]+\.\d{2}|[\d,]+)/; currency NGN/USD/EUR/GBP default NGN; dateRaw match DD/MM/YYYY or YYYY-MM-DD normalize via date-fns parse+isValid to YYYY-MM-DD; sender = remaining tokens via stripped text or extractSender remaining; if !amount || !date return null (ask to clarify).
    - Helper extractPdfText(buffer:Buffer):Promise&lt;string&gt; using pdf-parse: `const data = await pdf(buffer)` try/catch → '' trimmed; if text.length&gt;=40 return text else '' signals fallback.
    - Export async handleVerify(opts:{chatId:string; fileId?:string; mime?:string; caption?:string; freeFormText?:string; isPdf?:boolean}):Promise&lt;string&gt; orchestrating: if fileId present → downloadTelegramFile → tmpWriteWithHash → dedup Map check if isDuplicate → return cached `Already verified: ...` no AI/DB; then branch by mime: if isPdf → extractPdfText if &gt;=40 parse locally via parseFreeForm on pdfText, else call openRouterPdf; if image → if caption parses via parseFreeForm return that else call openRouterVision with buffer.toString('base64'); set verifyCache after extraction. For freeFormText without fileId → parseFreeForm directly, only if null call openRouterTextParse. After extraction, validate amount&gt;0 currency enum date YYYY-MM-DD via zod/date-fns, else ask clarify "Need amount and date — e.g. '100k 2026-09-09 SAMPLE SENDER'".
    - Deterministic match: function deterministicMatch(params): query `SELECT amount::text,currency,transaction_date::text,sender_name,description,available_balance::text,branch FROM transactions WHERE amount = $1::numeric AND transaction_date = $2::date AND sender_name ILIKE $3 LIMIT 5` with amountStr, date, senderLike `%sender%`. Handle 0,1,&gt;1 rows.
    - Near-matches on 0: query `SELECT amount::text,currency,transaction_date::text,sender_name,description, similarity(sender_name,$2) as sim FROM transactions WHERE amount=$1::numeric AND transaction_date BETWEEN ($3::date - INTERVAL '1 day') AND ($3::date + INTERVAL '1 day') AND similarity(sender_name,$2) &gt; 0.3 ORDER BY sim DESC LIMIT 3` GIN-backed. Return status FOUND/MULTIPLE/NOT_FOUND_NEAR/NOT_FOUND.
    - Format reply: FOUND → `✅ FOUND — 100.00 NGN from EXAMPLE MERCHANT on 2026-09-10 • Avail 319,599.78 • <description snippet>` with escapeHtml; MULTIPLE → list 2-5 matches; NOT_FOUND → `❌ Not found — no matching Zenith transaction yet` + if near-matches include top 3 suggestion block; + if non-Zenith receiptDetected prepend `ℹ️ Non-Zenith receipt — searching Zenith ledger 999****999`. All replies slice 4000. Cache final reply in verifyCache keyed by contentHash or freeForm key.
    - Rate limit 5/60s for verify handled in commands.ts gate before calling handleVerify; this module assumes session already checked.

    Extend src/telegram/commands.ts:
    - Import handleVerify, isDuplicate/cacheVerify not needed externally, getLargestPhotoId, downloadTelegramFile indirectly via verify.ts.
    - Add handler entry verify: async (chatId, args) => { if (isRateLimited(chatId,'verify',5,60000)) return '⏳ Verify cooling down — retry in ~30s'; /* text path */ const freeText = args.join(' '); if (freeText) return handleVerify({chatId, freeFormText:freeText}); return 'Send a photo/PDF with caption or type /verify 100k 2026-09-09 SAMPLE SENDER'; }.
    - Extend dispatch to handle Telegram message types beyond text: in handleTelegramUpdate, check u.message.photo (take largest via getLargestPhotoId), u.message.document (mime_type file_id), u.message.caption, u.message.text. If photo/document present and isLoggedIn, offload to handleVerify with fileId and caption, 5/60s gate, setImmediate would exceed tracer — instead run inline but fast (fail fast on dedup/20MB). For photo messages without /verify prefix, treat as implicit verify if logged in (admin convenience): if photo/document and isLoggedIn and not rate limited, call handleVerify. Ensure photo/document verify also requires login gate before download (no token burn for unauth).
    - Wire callback pagination not needed here.

    Keep 4096 cap, escapeHtml, never log raw sender_account full. Reuse existing pool, date-fns, zod.
  </action>
  <verify>
    <automated>npm test -- tests/telegram/verify.test.ts tests/telegram/media.test.ts --reporter=verbose</automated>
  </verify>
  <done>Free-form 100k→100000 + date regex local-first with AI only on miss; image caption parsed locally else OpenRouter vision image_url base64; PDF pdf-parse ≥40 local else openRouterPdf file_data mistral-ocr; all paths deterministic SELECT exact match + near-matches similarity>0.3 date±1 top3; SHA256 dedup returns cached Already verified within 24h; non-Zenith note added; 20MB/missing file_path friendly error; OPENROUTER_API_KEY missing degrades to text-only.</done>
</task>

<task type="auto">
  <name>Worker integration for photo/document dispatch + rate-limit + verify tests</name>
  <files>src/worker.ts, src/telegram/commands.ts, tests/telegram/verify.test.ts, tests/telegram/media.test.ts</files>
  <read_first>src/worker.ts:300-385 (telegram webhook branch with isAllowedChat replacement, rate limits, handleTelegramUpdate, 200 ACK, callback_query answer), src/telegram/session.ts (isLoggedIn), src/telegram/verify.ts (handleVerify just created), src/telegram/rateLimit.ts:1-30</read_first>
  <action>
    Verify src/worker.ts telegram branch correctly handles new verify photo/document flow: ensure extractChatId handles callback_query.photo cases, ensure parseCommandText handling for captions (caption may contain "/verify ..." or free-form), ensure isLoggedIn gate runs before any media download — unauth photo should not trigger getFile. Keep always-200 ACK pattern and sendTelegramMessage follow-up even when verify handler throws (suppressed). Extend _resetWorkerStateForTests to clear verifyCache and media tmp sweeper state.

    Ensure src/telegram/commands.ts dispatch handles three entry points: (a) /verify free-form text via handler map, (b) message with photo/document + caption containing /verify or implicit verify — route to handleVerify; (c) plain text starting with /verify but no media — treat as free-form verify. For each, apply isRateLimited(chatId,'verify',5,60000) gate returning cooldown reply before download. Ensure authenticated check uses isLoggedIn(chatId) not allowlist; unauth photo gets same login prompt as text.

    Extend tests: tests/telegram/media.test.ts covers getLargestPhotoId largest file_size, empty→null, downloadTelegramFile mocked fetch: success returns buffer, missing file_path → throws with friendly message, >20MB → throws, 429/timeout → throws; tmpWriteWithHash creates file with SHA256 name and fake timers advance 24h triggers unlink (vi.useFakeTimers + mock fs unlink). tests/telegram/verify.test.ts covers parseFreeForm 100k→100000, NGN default, DD/MM and ISO dates, missing amount/date→null/ask clarify; dedup Map second call returns Already verified without second fetch (mock fetch call count); PDF branch: mock pdf-parse text≥40 uses local parse no OpenRouter call, text&lt;40 calls openRouterPdf mocked; vision gate: caption parses → no openRouterVision call count 0, caption fails → vision called once; deterministicMatch mocked pool.query for FOUND (1 row), MULTIPLE (>1), NOT_FOUND_NEAR with similarity; non-Zenith note detection. Use vitest vi.mock for fetch, pdf-parse, pg Pool.

    No new deps; reuse vitest fake timers, mocked fetch global.
  </action>
  <verify>
    <automated>npm test -- tests/telegram/verify.test.ts tests/telegram/media.test.ts --reporter=verbose</automated>
  </verify>
  <done>Worker telegram branch routes photo/document/caption and /verify text through logged-in + 5/60s rate-limited handleVerify without losing 200 ACK; unauth media never downloads; media/verify tests cover download 2-step, 20MB guard, SHA256 24h tmp, dedup cache, pdf-parse gate, vision gate, deterministic SELECT and near-matches; all green with mocked fetch/pg.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Telegram photo/document file_id → getFile → /file/ download | Untrusted file bytes; 20MB cap + file_path existence + SHA256 before processing is trust boundary |
| OpenRouter vision/PDF response → JSON | Untrusted LLM JSON; strict zod/parse validation before DB bind is boundary |
| User free-form text/caption → parseFreeForm | Untrusted; regex + isValid before SQL bind |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-08 | Tampering | Arbitrary file_id download via verify | high | mitigate | Only file_id from own webhook message.photo/document trusted; validate file_path exists, cap 20MB, SHA256 before OpenRouter; per-chat verify 5/60s rate limit |
| T-02-09 | Tampering | OpenRouter prompt injection / hallucinated JSON | high | mitigate | Fixed vision prompt + response_format json_object temperature 0; validate amount numeric>0, currency enum, date isValid before parameterized SQL; never interpolate AI strings |
| T-02-10 | Information Disclosure | Temp image persist beyond 24h on disk | medium | mitigate | tmpWriteWithHash setTimeout 24h unref + boot sweep of tg-verify-* older than 24h; no S3/log persistence; unlink on dedup hit |
| T-02-11 | Spoofing | Non-Zenith receipt spoofed as Zenith | medium | mitigate | Always searches Zenith transactions only (D-06) with transparent note "Non-Zenith receipt — searching Zenith ledger 999****999"; never trusts receipt bank |
| T-02-12 | Tampering | Deterministic match fuzzy amount / date drift | medium | mitigate | Exact amount NUMERIC compare + exact transaction_date DATE; near-matches are suggestions only (similarity>0.3 + date ±1) never auto-confirm |
| T-02-13 | Denial of Service | Repeated same-image verify burns OpenRouter | medium | mitigate | SHA256 content_hash dedup Map 24h check before AI/DB; second hit returns cached Already verified text |
| T-02-14 | Information Disclosure | Log full sender_account / raw_email | medium | mitigate | Pino bindings only chatId + truncated contentHash + amount/date/senderName; never log raw buffer base64 or full account |
| T-02-SC | Tampering | pdf-parse npm package legitimacy | high | mitigate | Registry verified pdf-parse@2.4.5 age ~9yrs 180K/wk, postinstall null; npm view verified before install in plan 02-01 media task; single new dep only |

</threat_model>

<verification>
- `npm test -- tests/telegram/verify.test.ts tests/telegram/media.test.ts` green (mocked fetch for getFile/download, fake timers for 24h unlink, mocked pdf-parse and pg pool).
- Manual: with TELEGRAM_BOT_PASSWORD and OPENROUTER_API_KEY set, login then send photo with caption "100k 2026-09-10 EXAMPLE MERCHANT" → reply FOUND with balances; resend same photo within 24h → "Already verified: ..."; send same photo but OPENROUTER_API_KEY unset still works via caption parse; send PDF text-based → pdf-parse path no OpenRouter call (verified via logs); send free-form "/verify 100k 2026-09-09 SAMPLE SENDER" → local regex FOUND/NOT_FOUND with near-matches if amount matches ±1d.
- Temp file exists in os.tmpdir after verify, deleted after 24h (fake timer) and via boot sweep.
- 20MB limit: send document with file_size >20MB returns friendly max message without download attempt.
</verification>

<success_criteria>
- 2-step getFile (POST getFile then GET /file/bot<TOKEN>/<file_path>) works for photo largest file_size and document PDF with 20MB cap and missing file_path friendly error.
- SHA256 content_hash dedup Map returns Already verified within 24h without re-burning OpenRouter or DB; temp file 24h lifecycle proven via fake timers + boot sweep.
- pdf-parse text≥40 parsed locally, &lt;40 falls back to OpenRouter file_data with mistral-ocr plugin; image caption local parse gates vision (vision only when caption fails and key present).
- Free-form text 100k/DD/MM/ISO parsed locally via date-fns; AI text parse only when local returns null.
- Deterministic SELECT amount exact + transaction_date exact + sender_name ILIKE with FOUND/MULTIPLE/NOT_FOUND + near-matches similarity>0.3 date±1 top3 and non-Zenith note; all SQL parameterized.
- Only one new runtime dep added: pdf-parse@2.4.5 (verified postinstall null); OpenRouter via raw fetch, no openai SDK.
</success_criteria>

<output>
Create `.planning/phases/02-telegram-ledger-assistant/02-02-SUMMARY.md` when done
</output>
