---
phase: 07-ux-overhaul
plan: 03
type: execute
wave: 3
depends_on:
  - 07-02
files_modified:
  - src/telegram/history.ts
  - src/telegram/search.ts
  - src/telegram/commands.ts
  - src/telegram/webhook.ts
  - src/worker.ts
autonomous: true
requirements:
  - 07-R8
  - 07-R9
estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "History date range accepts natural language 'history last week' / 'history September' parsed via OpenRouter gemma to from/to YYYY-MM-DD Africa/Lagos, explicit DD/MM or YYYY-MM-DD remains fast path no AI cost (per D-08)"
    - "Search and history both trigger without slash: 'search SAMPLE SENDER 100k' and 'history last week' when logged in route to same handlers as /search /history (per D-16)"
    - "History and search results paginate via inline Next 10 / Prev with callback_data ≤64B and answerCallbackQuery within 30s; card-per-TX 10/page under 4096 (per D-01 + RESEARCH pitfalls 1/2)"
  artifacts:
    - src/telegram/history.ts natural-language branch trying parseLagosDateRange first then gemma fallback
    - src/telegram/search.ts slash-less entry already + truncated callback pagination
    - src/worker.ts slash-less routing before startsWith('/') early return + callback pagination handler
  key_links:
    - "history ... args where parseLagosDateRange returns error and text contains words -> openRouterSearchIntent(today Africa/Lagos) -> fromDate/toDate -> handleHistoryWithRange(from,to) ; on 429/null fallback to localKeywordIntent / explicit parse"
    - "handleTelegramUpdate message.text without '/' -> if loggedIn and text matches /^search\\s+/i or /^history\\s+/i -> route to handleSearchStub/handleHistoryWithRange with answerCallback; else return null"
    - "Inline Next/Prev -> callback_data '/history from to 10 offset' or '/search <truncated30> offset' -> handleHistoryWithRange / handleSearch with offset -> editMessageText + answerCallbackQuery"
---

<objective>
Natural-language history + slash-less search/history + inline pagination polish (per D-08/D-16/D-01): gemma parses "last week"/"September" to Lagos range, bare "search …" and "history …" work without slash, and history/search paginate 10/page with Next/Prev ≤64B and spinner dismissal.

Purpose: Make ledger queries chat-like and phone-usable with zero new deps — the most requested ergonomic win per "search is very important".
Output: NL history via gemma fallback, slash-less entry, pagination inline keyboards.
</objective>

<execution_context>
@C:/Users/user/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/user/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-ux-overhaul/07-CONTEXT.md
@.planning/phases/07-ux-overhaul/07-RESEARCH.md
@src/telegram/history.ts
@src/telegram/search.ts
@src/telegram/commands.ts
@src/telegram/webhook.ts
@src/worker.ts
@src/telegram/openrouter.ts
</context>

<tasks>

<task type="auto">
  <name>Natural-language history via gemma with explicit fast path</name>
  <files>src/telegram/history.ts, src/telegram/openrouter.ts</files>
  <read_first>src/telegram/history.ts:1-65 (parseLagosDateRange + parseDateArg DD/MM + YYYY-MM-DD + 4-arg pagination form), src/telegram/search.ts:1-120 (openRouterSearchIntent gemma-3-27b-it prompt with today Africa/Lagos, localKeywordIntent fallback, zod SearchIntentSchema), src/telegram/search.ts:300-420 (handleSearch limit/offset + inline Next), src/telegram/openrouter.ts:1-90 (existing OpenRouter helper fetch shape, timeout), src/telegram/commands.ts:589-598 (history handler delegates to handleHistoryWithRange)</read_first>
  <action>
    In src/telegram/history.ts add NL branch per RESEARCH Pattern 6/ Pitfall 8 (per D-08): Before calling OpenRouter, try explicit parse first: if args length ===2 and parseLagosDateRange(args) succeeds (no error) then use from/to immediately with no AI cost. Only if explicit parse fails and args joined contains at least one letter word (e.g., "last week", "September", "this month") then call openRouterSearchIntent style helper: reuse existing src/telegram/search.ts openRouter intent or src/telegram/openrouter.ts generic — pass today = new Date().toLocaleDateString('en-CA',{timeZone:'Africa/Lagos'}) as YYYY-MM-DD context. Extract only fromDate/toDate from returned intent; ignore sender/amount. Validate from/to via zod regex ^\d{4}-\d{2}-\d{2}$ and from<=to; if AI returns null or 429/timeout, fallback to local heuristic: parse "last week" as 7d range ending today, "September"/month names as that month's 01 → lastDay in Africa/Lagos via date-fns, else show error "Invalid date — try DD/MM/YYYY or 'last week'". Keep BETWEEN $from::date AND $to::date LIMIT 10 OFFSET pagination. Cap AI calls to one per history request; log warn on fallback. Do not add new AI model param beyond google/gemma-3-27b-it already in repo.

    Preserve 4-arg callback_data form "/history from to limit offset" parsing at top of handleHistoryWithRange so pagination never re-triggers AI. Ensure card renderer from 07-01 remains (card-per-TX). Keep withTimeout 5000 for DB.

    Do NOT call AI for YYYY-MM-DD or DD/MM/YYYY explicit dates — fast path must bypass fetch to OpenRouter entirely.
  </action>
  <verify>
    <automated>npm run lint 2>&1 | tail -20</automated>
  </verify>
  <done>Explicit history DD/MM|ISO bypasses AI; natural language "last week"/"September" triggers gemma with Africa/Lagos today and falls back to local parser on 429; pagination offset avoids re-calling AI.</done>
</task>

<task type="auto">
  <name>Slash-less search and history entry when logged in</name>
  <files>src/telegram/commands.ts, src/telegram/webhook.ts, src/worker.ts</files>
  <read_first>src/telegram/commands.ts:609-713 (handleTelegramUpdate message.text startsWith('/') guard + callback_query dispatch + media verify branch), src/telegram/webhook.ts:28-38 (parseCommandText lowercases + strips @Bot), src/worker.ts:320-365 (session gate earlyCmd publicCmds check + handleTelegramUpdate dispatch + reply forwarding)</read_first>
  <action>
    Implement D-16 slash-less chat-like entry per RESEARCH Pattern 7: In handleTelegramUpdate, add branch BEFORE the 'if (!text.startsWith('/')) return null' early return that handles bare prefix when logged in. Steps: after media branch, if message?.text exists and isLoggedIn(chatId) is true, trim text and test /^search\s+/i and /^history\s+/i without slash (case-insensitive). If matches "search …", extract queryText = text.slice(6).trim() and route to handleSearchStub(chatId, [queryText] or split words) same as /search; if matches "history …", extract rest = text.slice(7).trim(), split into args and route to handleHistoryWithRange(args). Return result same as slash path. If text is exactly "search" or "history" with no args, return usage hint. Do NOT trigger for unauthenticated — session gate already returns Welcome; slash-less branch must also guard with isLoggedIn. Do NOT treat bare "SAMPLE SENDER 100k" without prefix as search — limit to explicit search/history prefix per RESEARCH Open Question 1 recommendation.

    Ensure callback_data handling remains for inline Next/Prev: parseCommandText already lowercases but history/search dispatch is case-insensitive via early regex.

    Keep existing slash commands untouched; slash-less is additive routing that reuses same stubs/handlers.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Bare "search SAMPLE SENDER 100k" and "history last week" when logged in produce same results as slash variants; unauth still gets Welcome; prefix-less bare words do not false-trigger.</done>
</task>

<task type="auto">
  <name>Inline pagination for history/search with ≤64B callback and spinner dismissal</name>
  <files>src/telegram/history.ts, src/telegram/search.ts, src/telegram/commands.ts, src/worker.ts</files>
  <read_first>src/telegram/history.ts:159-207 (formatRows navRow Prev Next callback_data "/history from to limit offset"), src/telegram/search.ts:300-420 (handleSearch pagination offset + callback_data encode truncated 60 + offset), src/worker.ts:398-406 (answerCallbackQuery after reply)</read_first>
  <action>
    Harden pagination per RESEARCH Pitfalls 1-3: In history.ts ensure callback_data generation keeps ≤64B: use form `/history ${from} ${to} ${limit} ${offset}` where from/to are YYYY-MM-DD (10 each) so total ~36B. For default isDefault history (no range), use `/history ${limit} ${offset}` or `/history 5 10` short form — ensure no 4-arg form with empty from/to produces longer string. In search.ts handleSearch pagination, keep callback_data as `/search ${encodeURIComponent(query.slice(0,30))} ${offset}` where query truncated to 30 chars before encode to stay under 64B even after % encoding (per Pitfall 1). If query still exceeds boundary, store not needed — truncate dominates. Ensure handler stubs decode correctly: handleSearchStub decodes via decodeURIComponent with try/catch.

    Ensure every callback_query path calls answerCallbackQuery: worker already has token-guarded fetch POST bot/answerCallbackQuery after reply — keep but also ensure handleTelegramUpdate callback branch triggers answer even on error branches. In edit flow from 07-02 placeholder for history pages, also ensure editMessageText path answers callback.

    Verify 4096 handling: history already drops whole cards; search should similarly chunk to <4000 before send. Test Next taps do not re-invoke OpenRouter for history date range — pagination passes from/to explicitly so NL is skipped.

    Keep worker single server, no new deps.
  </action>
  <verify>
    <automated>npm run lint 2>&1 | tail -20</automated>
  </verify>
  <done>History and search inline Next/Prev callback_data stays ≤64B, truncated query ≤30 chars, answerCallbackQuery dismissed on every tap, AI not re-invoked on pagination, cards stay under 4096.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User text without slash → search/history parser | Untrusted; AI-parsed dates must be validated |
| callback_data (64B) → pagination offset | Untrusted client-controlled; must parse as numbers only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-12 | Tampering | NL history AI from/to injection | high | mitigate | Zod validate from/to YYYY-MM-DD regex + from<=to; never interpolate raw AI text; parameterized $1::date BETWEEN |
| T-07-13 | Tampering | callback_data offset injection | medium | mitigate | Parse offset with /^\d+$/ and clamp 0..total; never eval callback_data as SQL |
| T-07-14 | Information Disclosure | slash-less search leaking when unauth | medium | mitigate | Branch guarded by isLoggedIn(chatId) check before routing; unauth gets Welcome no DB |
| T-07-15 | Denial of Service | AI cost on every /history even with explicit dates | medium | mitigate | Fast path parseLagosDateRange first; only call gemma when words present and explicit parse failed; single call per request |
| T-07-SC | Tampering | npm installs | high | mitigate | Zero new deps — google/gemma-3-27b-it via existing fetch + date-fns already installed |
</threat_model>

<verification>
- Lint/build pass, single PORT 8080.
- Stub OpenRouter to return {fromDate:"2026-09-01", toDate:"2026-09-07"} for "last week" with today 2026-09-09 Africa/Lagos — history last week resolves to range and renders cards; explicit "01/09/2026 10/09/2026" does not call fetch (spy count 0).
- Bare "search SAMPLE SENDER 100k" when logged in hits handleSearch with query "SAMPLE SENDER 100k"; same text when not logged in returns Welcome card.
- Tap Next 10 on history where total 25: first call offset 0 shows cards 1-10 + Next; second tap callback_data "/history 2026-09-01 2026-09-10 10 10" renders offset 10 cards 11-20 + Prev+Next; all callback_data length checked ≤64B via `Buffer.byteLength(cb,"utf8") <=64`.
- answerCallbackQuery fetch spy shows one call per callback_query including pagination and status taps, even on error branch.
- 4096: history page with long branch strings stays <4000 after whole-card drop, not mid-card slice.
</verification>

<success_criteria>
- Natural language history "last week"/"September" resolves via gemma with Lagos today to from/to and renders cards; explicit dates remain no-AI.
- Slash-less "search …" and "history …" work when logged in identically to slash variants.
- Pagination keyboards use short callback_data ≤64B with truncated query ≤30 chars, offset clamped, and spinner dismissed via answerCallbackQuery.
</success_criteria>

<output>
Create `.planning/phases/07-ux-overhaul/07-03-SUMMARY.md` when done
</output>
