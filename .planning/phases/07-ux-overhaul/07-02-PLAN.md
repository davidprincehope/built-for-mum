---
phase: 07-ux-overhaul
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/telegram/sendMessage.ts
  - src/telegram/verify.ts
  - src/telegram/commands.ts
  - src/zenith/sender.ts
  - src/worker.ts
autonomous: true
requirements:
  - 07-R5
  - 07-R6
  - 07-R7
estimate:
  tokens: 58000
  raw_tokens: 29000
  tasks: 2
  confidence: med
must_haves:
  truths:
    - "Found verify shows rich card '✅ VERIFIED — 100.00 NGN from EXAMPLE MERCHANT • 2026-09-10 • via NIP/KUDA • Available: 309,599.78' with inline View History (per D-09)"
    - "Verify sends ⏳ Verifying… instantly then editMessageText to FOUND/NOT_FOUND; only bot's own message is edited; message is not modified is swallowed; answerCallbackQuery always called for callbacks (per D-12)"
    - "Blank transaction_reference for NIP/KUDA shows cleaned sender only via extractSender, not raw NIP/KUDA/... (per D-11)"
    - "NOT_FOUND shows 'Not found — no matching Zenith transaction yet' plus top 3 near-matches when amount exact + sender similarity >0.3 + date ±1 (per D-10)"
  artifacts:
    - src/telegram/verify.ts exporting enriched FOUND/NOT_FOUND renderers + near-matches query logic reusing extractSender
    - src/telegram/sendMessage.ts editTelegramMessage + answerCallbackQuery handling Verifying then edit flow
    - src/worker.ts webhook handling callback answer + Verifying placeholder wiring for media verify
  key_links:
    - "photo/document when isLoggedIn -> sendMessageWithId('⏳ Verifying…') capture message_id -> handleVerify (OpenRouter only when necessary + deterministic SELECT + near-matches) -> editTelegramMessage(chatId, placeholderId, rich card) + answerCallbackQuery"
    - "handleVerify FOUND path -> near-matches SELECT amount=:amount AND transaction_date ±1 + pg_trgm similarity on sender/description threshold 0.3 -> top 3"
    - "Renderer -> extractSender(description).senderName cleaned for blank ref; raw NIP/KUDA/... never shown"
---

<objective>
Verify experience polish (per D-09/D-10/D-11/D-12): rich FOUND card with checkmark, Verifying… immediate reply then edit illusion, cleaned sender for blank refs, and near-matches on NOT_FOUND — reusing existing worker PORT 8080 and pino, no new deps.

Purpose: Make every phone-triggered verify feel instant and professional (the highest-friction admin flow) while preserving deterministic matching on transactions ledger.
Output: Rich cards for FOUND/NOT_FOUND, Verifying… → edit, cleaned sender, near-matches threshold.
</objective>

<execution_context>
@C:/Users/user/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/user/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-ux-overhaul/07-CONTEXT.md
@.planning/phases/07-ux-overhaul/07-RESEARCH.md
@src/telegram/verify.ts
@src/telegram/sendMessage.ts
@src/telegram/commands.ts
@src/worker.ts
@src/zenith/sender.ts
@src/telegram/search.ts
</context>

<tasks>

<task type="auto">
  <name>Rich FOUND/NOT_FOUND cards + cleaned sender + near-matches</name>
  <files>src/telegram/verify.ts, src/zenith/sender.ts</files>
  <read_first>src/telegram/verify.ts:1-200 (handleVerify freeFormText + fileId flow, deterministic SELECT, image/PDF to OpenRouter, return string), src/telegram/verify.ts:36-80 (verifyCache dedup 24h Map hash->result), src/zenith/sender.ts:60-135 (extractSender four families returning senderName/senderAccount), src/telegram/sendMessage.ts:1-30 (escapeHtml already), src/telegram/search.ts:200-320 (pg_trgm similarity use if present), src/telegram/commands.ts:80-97 (handleVerifyStub)</read_first>
  <action>
    In src/telegram/verify.ts build rich renderers without changing matching logic: Keep existing deterministic SELECT on transactions (amount/currency/date/sender) — do not add new business rules beyond presentation. Add helper renderFoundCard(matchRow): returns "✅ &lt;b&gt;VERIFIED&lt;/b&gt; — &lt;code&gt;amount currency&lt;/code&gt; from &lt;code&gt;cleanedSender&lt;/code&gt; • &lt;code&gt;YYYY-MM-DD&lt;/code&gt; • via &lt;b&gt;NIP|KUDA|Zenith&lt;/b&gt; • Available: &lt;code&gt;available_balance&lt;/code&gt;" where cleanedSender = escapeHtml(extractSender(description).senderName || sender_name) and via derived from description.includes('NIP'/'KUDA'). Escape every DB string via escapeHtml. Add inline View History suggestion via replyMarkup returned alongside text (caller will forward). Keep per RESEARCH HTML allowed tags only <b><code><i><a>.

    For NOT_FOUND, return base "❌ &lt;b&gt;Not found&lt;/b&gt; — no matching Zenith transaction yet\n<i>Tip: check amount/sender/date or try /history 2026-09-01 2026-09-10</i>" then append near-matches: if amount was exact (parsed amount present) run a SELECT for candidates `SELECT amount::text, description, sender_name, transaction_date::text FROM transactions WHERE amount = $amount::numeric AND transaction_date::date BETWEEN ($date::date - interval '1 day') AND ($date::date + interval '1 day') LIMIT 20` then client-side compute simple sender similarity (Jaccard/trigram) and filter where similarity >0.3; show top 3 as "`amount currency • sender • date`" lines with cleaned sender via extractSender. If no amount/date parsed, skip near-matches. Cap 3, escape all. If pg_trgm not available fallback to ILIKE contains >0.3 threshold via crude includes check — keep 0.3 semantic.

    For blank transaction_reference handling (per D-11): wherever branch shows via NIP/KUDA with empty reference, show cleaned sender only (extractSender) not raw "NIP/KUDA/EXAMPLE MERCHANT/Cash". Do not show both variants. This applies to both FOUND and NOT_FOUND near-matches display.

    Keep verifyCache dedup and timing; do not add new storage. Do not add new transport; return {text, replyMarkup} shape if caller expects string — ensure backward compat: if existing caller expects string, return text string but also expose render helpers that include replyMarkup for worker edit path. Prefer updating handleVerify return to string | {text, replyMarkup} and updating commands stub to handle both, matching existing pattern from 02-02.

    Do not introduce pdf-parse or vision changes — reuse existing local-first then OpenRouter only when necessary path unchanged (per CONTEXT no new business logic beyond presentation).
  </action>
  <verify>
    <automated>npm run lint 2>&1 | tail -20</automated>
  </verify>
  <done>FOUND renders as rich checkmark card with via + date + Available and View History inline; NOT_FOUND shows base + up to 3 near-matches when amount exact + similarity >0.3 + date ±1; cleaned sender only via extractSender for blank refs; all strings escaped.</done>
</task>

<task type="auto">
  <name>Verifying… then edit illusion + any image = verify wiring</name>
  <files>src/telegram/sendMessage.ts, src/telegram/commands.ts, src/worker.ts</files>
  <read_first>src/worker.ts:307-412 (webhook media branch photo||document -> isLoggedIn gate -> getLargestPhotoId -> handleVerify -> sendTelegramMessage; callback answer already), src/telegram/commands.ts:609-712 (handleTelegramUpdate media verify branch already any image = verify per D-07, caption strip /verify), src/telegram/sendMessage.ts:1-102 (new helpers from 07-01: sendWithId/edit/answer exist after wave 1)</read_first>
  <action>
    In src/worker.ts refactor the media verify and command verify paths to use Verifying then edit per D-12 + RESEARCH Pattern 4: Before awaiting handleVerify, call sendTelegramMessageWithId(chatId, "⏳ &lt;b&gt;Verifying&lt;/b&gt; 100.00 NGN… &lt;i&gt;extracting…&lt;/i&gt;", {parseMode:'HTML'}) and capture placeholderId (number|null). If placeholderId null (token missing), just proceed to send final. After handleVerify resolves to reply (string or {text,replyMarkup}), if placeholderId exists call editTelegramMessage(chatId,placeholderId,reply.text,{replyMarkup:reply.replyMarkup ?? {inline_keyboard:[[{text:"📜 View History",callback_data:"/history 5"}]]}}) — ensure inline includes View History for FOUND. If edit returns swallow (message is not modified already handled inside helper), fallback to sendTelegramMessage if edit failed due to "message to delete not found" shape. For text free-form verify (caption or /verify args), apply same Verifying→edit when possible: the worker already sends reply via sendTelegramMessage after handleTelegramUpdate — to implement Verifying there, intercept in handleTelegramUpdate or worker: if cmd is verify with args, send Verifying placeholder before calling handleVerifyStub then edit. Simplest: worker does generic Verifying for any verify-initiated path where handleTelegramUpdate returns a verify reply — but to keep minimal risk, wire Verifying only for media branch (photo/document) where latency is highest (OpenRouter vision 2-5s per D-12). For /verify text with freeForm, the existing handleVerifyStub is fast (local parse) so Verifying is optional.

    Preserve D-07 "any image = verify" already in commands.ts:609-696 auto-triggers verify when logged in without requiring caption "verify" — keep guard isLoggedIn before download, rateLimit 5/60s, getLargestPhotoId, mime resolve. Do not add caption==verify requirement; any photo/document triggers.

    Ensure answerCallbackQuery is always called for callback_query id before or after edit — reuse the helper from 07-01; even if edit succeeded, answer to dismiss spinner. Swallow 400 there.

    Keep friendly error handling: verify failures return "⚠️ Verify failed — try again" not stack.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Media verify path sends Verifying… within <100ms then edits to rich FOUND/NOT_FOUND with inline; only bot's own message is edited; message is not modified swallowed; callback spinner dismissed; any image = verify stays intact per D-07.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telegram photo/document file_id → getFile → /file download | Untrusted file content; size 20MB cap, hash dedup |
| Postgres transactions rows → rich card HTML | DB trusted but description may contain injection chars |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-08 | Tampering | verify photo/PDF download | high | mitigate | Reuse existing 20MB file_size check, SHA256 tmp dedup 24h, never execute file, pdf-parse local-first |
| T-07-09 | Tampering | HTML injection via description in FOUND card | medium | mitigate | escapeHtml every DB field before <b><code>; retry without parse_mode on can't parse entities |
| T-07-10 | Information Disclosure | near-matches leaking non-matching rows | low | accept | Near-matches only shows amount/sender/date already visible via /history; gated behind login |
| T-07-11 | Denial of Service | Verifying edit flood if user spams images | medium | mitigate | Keep verify 5/60s per-chat rateLimit before any download; placeholder edit is one extra Bot API call per verify only |
| T-07-SC | Tampering | npm installs | high | mitigate | Zero new deps; reuse Node fetch/crypto + existing pdf-parse/pg/pino |
</threat_model>

<verification>
- Lint/build pass, single PORT 8080 unchanged.
- Mock fetch spy sequence for photo update when logged in: capture POST bot/sendMessage "Verifying" -> returns {result:{message_id:77}} -> then handleVerify mock -> capture POST bot/editMessageText {chat_id, message_id:77, text:"✅ VERIFIED"}; verify only one send + one edit, not two sends.
- FOUND card regex: /✅.*VERIFIED.*NGN.*via (NIP|KUDA)/ and cleaned sender not containing "/".
- NOT_FOUND without amount shows no near-matches section; with amount 100.00 + date within ±1 shows up to 3.
- Raw description with "<" is escaped not breaking parse_mode; edit's can't parse entities retry works.
- Any photo without caption when logged in triggers verify (no caption==verify check).
- callback_query Next tap still calls answerCallbackQuery (verified via fetch spy) spinner dismissed.
</verification>

<success_criteria>
- FOUND card shape exactly per D-09 with checkmark + via + date + Available + inline View History.
- Verifying… placeholder appears instantly then edited to final (not second send) with swallowed not-modified.
- Cleaned sender via extractSender only for blank refs (no raw NIP/KUDA/EXAMPLE MERCHANT/Cash shown).
- NOT_FOUND triggers near-matches SQL filtered by amount exact + similarity >0.3 + date ±1, top 3.
</success_criteria>

<output>
Create `.planning/phases/07-ux-overhaul/07-02-SUMMARY.md` when done
</output>
