---
phase: 07-ux-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/telegram/sendMessage.ts
  - src/telegram/menu.ts
  - src/telegram/session.ts
  - src/telegram/commands.ts
  - src/telegram/history.ts
  - src/telegram/balance.ts
  - src/observability/logger.ts
  - src/worker.ts
autonomous: true
requirements:
  - 07-R1
  - 07-R2
  - 07-R3
  - 07-R4
estimate:
  tokens: 62000
  raw_tokens: 31000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "Unauthenticated user gets Welcome card 'Welcome to PaymentVerificationBot 🤖 — Send /login <password> to start (24h session) • Try /help' with inline Login button, zero DB touch (per D-13)"
    - "After /login <password> verified via timingSafeEqual, bot calls deleteMessage on that message_id in same tick (best-effort, 400/429 swallowed) and never logs password/text (per D-17)"
    - "History renders as card-per-transaction (💳 Amount, 👤 Sender via extractSender, 📅 Date Africa/Lagos, 🏦 via NIP/KUDA, 📝 Description cleaned, 🔖 Branch, 💰 Balance) capped 10 per page, phone-friendly, not pre table (per D-01)"
    - "Persistent Menu Button shows 7 commands always visible via setMyCommands + setChatMenuButton {type: commands} registered on boot idempotently (per D-05/D-15)"
    - "All errors are friendly with emoji + hint e.g. '⏳ Slow down — you hit 30/min, try again in 12s. Tip: use /history 2026-09-01' not technical (per D-14)"
  artifacts:
    - src/telegram/sendMessage.ts exporting deleteTelegramMessage + editTelegramMessage + answerCallbackQuery + sendTelegramMessageWithId reusing getBotToken/fetch/429/400 patterns
    - src/telegram/menu.ts exporting registerMenuIfConfigured calling setMyCommands 7 + setChatMenuButton global + getMyCommands health log
    - src/telegram/history.ts rewritten to card-per-TX renderer with 10/page divider ━━━━━━━━━━━━ and escapeHtml on all DB strings
    - src/worker.ts createHttpServer still single http.createServer on PORT 8080 with registerMenuIfConfigured on boot + updated unauth Welcome + login delete wiring
  key_links:
    - "POST /telegram/webhook -> verifySecretToken -> extractChatId + message_id -> session gate -> if /login success -> deleteMessage(chatId,message_id) fire-and-forget -> Welcome/Login reply"
    - "Worker start() -> registerTelegramWebhookIfConfigured() -> registerMenuIfConfigured() -> setMyCommands + setChatMenuButton sequential, boot continues on failure"
    - "/history dispatch -> handleHistoryWithRange -> SELECT ... LIMIT 10 OFFSET pagination -> renderTxCard via extractSender cleaned sender + escapeHtml -> 4096 safe chunk -> inline Next 10 keyboard"
    - "Unauthenticated -> Welcome card with inline Login (callback_data /login) -> no DB query, no pino password"
---

<objective>
Tracer that proves the UX overhaul skeleton end-to-end (per D-01/D-05/D-13/D-14/D-17): deleteMessage password hygiene, card-per-TX history, Welcome card, friendly errors, and Menu Button — all on the existing worker PORT 8080 reusing pino/fetch, zero new deps.

Purpose: Validate the most critical security + display primitives before verify polish and NL history; every later plan builds on these helpers and the Menu registration.
Output: Working deleteMessage on /login, card-per-TX history, Welcome card for unauth, friendly rate-limit errors, Menu Button visible in client.
</objective>

<execution_context>
@C:/Users/user/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/user/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-ux-overhaul/07-CONTEXT.md
@.planning/phases/07-ux-overhaul/07-RESEARCH.md
@src/worker.ts
@src/telegram/commands.ts
@src/telegram/sendMessage.ts
@src/telegram/webhook.ts
@src/telegram/session.ts
@src/telegram/history.ts
@src/telegram/balance.ts
@src/observability/logger.ts
@src/zenith/sender.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>End-to-end tracer: deleteMessage + Welcome card + card-per-TX + Menu Button + friendly errors</name>
  <files>src/telegram/sendMessage.ts, src/telegram/menu.ts, src/telegram/session.ts, src/observability/logger.ts, src/telegram/commands.ts, src/telegram/history.ts, src/telegram/balance.ts, src/worker.ts</files>
  <read_first>src/telegram/sendMessage.ts:1-102 (escapeHtml, getBotToken, sendTelegramMessage 4000 slice + 429 retry + 400 fallback), src/telegram/webhook.ts:1-59 (verifySecretToken, extractChatId handles callback_query, parseCommandText), src/telegram/session.ts:1-61 (Map chatId expiry 24h timingSafeEqual hourly sweep), src/telegram/commands.ts:50-110 (handleLogin timingSafeEqual wrong/correct), src/telegram/commands.ts:146-208 (buildHelpReply sections + inline), src/telegram/history.ts:1-207 (parseLagosDateRange + handleHistoryWithRange pre table to be replaced), src/telegram/balance.ts:1-90 (buildBalanceReply Available/Current), src/worker.ts:252-412 (registerTelegramWebhookIfConfigured + createHttpServer single PORT 8080 + session gate + global 15/10s), src/observability/logger.ts:1-93 (pino redact paths), src/zenith/sender.ts:60-135 (extractSender cleaned sender)</read_first>
  <action>
    Extend src/telegram/sendMessage.ts: keep sendTelegramMessage as-is; ADD 4 helpers reusing getBotToken/fetch patterns (per RESEARCH Pattern 2+4, D-12/D-17): 1) export async function deleteTelegramMessage(chatId, messageId): fetch POST bot&lt;TOKEN&gt;/deleteMessage {chat_id, message_id}, best-effort swallow 400/429 never throw, optional single 429 retry_after. 2) export async function sendTelegramMessageWithId(chatId,text,opts): same as sendTelegramMessage but returns message_id or null by parsing {ok,result:{message_id}}; reuse 4000 slice + 429 + can't parse entities fallback. 3) export async function editTelegramMessage(chatId,messageId,text,opts): POST bot/editMessageText {chat_id,message_id,text.slice0-4000,parse_mode HTML,disable_web_page_preview,reply_markup}, swallow "message is not modified" as success, on "can't parse entities" retry without parse_mode stripping tags, never throw. 4) export async function answerCallbackQuery(callbackQueryId,text?): POST bot/answerCallbackQuery {callback_query_id, text slice 200} swallow all errors. Share getBotToken extraction (TELEGRAM_BOT_TOKEN, TELEGRAM_BOT_TOKEN_ALT, ALERT_WEBHOOK_URL regex). No new deps.

    Create src/telegram/menu.ts per RESEARCH Pattern 3 (per D-05/D-15): export const COMMANDS = 7 entries help/status/balance/history/search/verify/summary with descriptions; export async function registerMenuIfConfigured(): if no token return; fetch POST bot/setMyCommands {commands:COMMANDS, scope:{type:"all_private_chats"}} log warn on !ok else info "setMyCommands ok"; then fetch POST bot/setChatMenuButton {menu_button:{type:"commands"}} global (omit chat_id) log warn/info; then GET bot/getMyCommands health log length; each fetch try/catch boot continues on failure. Use same getBotToken helper inline or import from sendMessage if exported, else duplicate tiny resolver. No new deps.

    Update src/observability/logger.ts pino redact: ensure TELEGRAM_BOT_PASSWORD, TELEGRAM_BOT_TOKEN, TELEGRAM_WEBHOOK_SECRET already present; add '*.text','*.password','text','password' to remove:true paths if not already, so login update.message.text never persists (per D-17). Keep multistream RingBufferStream.

    Rewrite src/telegram/history.ts renderer for card-per-TX (per D-01): keep parseLagosDateRange + parseDateArg as-is; replace formatRows to emit card strings: for each row build lines [💳 &lt;b&gt;amount currency&lt;/b&gt;, 👤 &lt;b&gt;Sender:&lt;/b&gt; &lt;code&gt;cleaned&lt;/code&gt; where cleaned=extractSender(description).senderName||sender_name, 📅 &lt;b&gt;Date:&lt;/b&gt; &lt;code&gt;YYYY-MM-DD&lt;/code&gt; &lt;code&gt;HH:MM&lt;/code&gt;? &lt;i&gt;Africa/Lagos&lt;/i&gt;, 🏦 &lt;b&gt;via NIP|KUDA|Zenith&lt;/b&gt; derived from description.includes, 🔖 &lt;b&gt;Branch:&lt;/b&gt; &lt;code&gt;branch&lt;/code&gt; if branch, 📝 &lt;i&gt;cleaned slice 60&lt;/i&gt;, 💰 &lt;b&gt;Balance:&lt;/b&gt; &lt;code&gt;available_balance&lt;/code&gt; if present] filter(Boolean).join('\n') per card; wrap each DB string with escapeHtml before interpolating. Join cards with "\n━━━━━━━━━━━━\n". Enforce 10 per message: SELECT already limits 10; if rendered text exceeds 3800 drop last whole cards then append "… + N more — tap Next 10" rather than slicing mid-card (per Pitfall 6). Keep pagination navRow Prev/Next via callback_data "/history from to 10 offset" (short ≤40B). Keep header "💳 Recent Transactions" vs "💳 History from → to". Keep 4096 safe. Keep withTimeout 5000.

    Similarly polish src/telegram/balance.ts card (per D-02 style alignment): add emojis/sections if not already — ensure output uses 💰 Available/Current + 📅 Last TX Africa/Lagos + escapeHtml. Keep ORDER BY transaction_date DESC, transaction_time DESC, created_at DESC LIMIT 1.

    Extend src/telegram/commands.ts: add buildWelcomeReply() returning {text: "🤖 &lt;b&gt;Welcome to PaymentVerificationBot&lt;/b&gt;\n━━━━━━━━━━━━━━━━━━━━\nSend &lt;code&gt;/login &lt;password&gt;&lt;/code&gt; to start (24h session)\n• Try &lt;code&gt;/help&lt;/code&gt; for commands", replyMarkup:{inline_keyboard:[[{text:"🔐 Login",callback_data:"/login"}],[{text:"❓ Help",callback_data:"/help"}]]}} (per D-13). Update handleLogin signature to (chatId,args,messageId?) and after login success if messageId provided call deleteTelegramMessage(chatId,messageId).catch(()=>{}) fire-and-forget immediately before returning text "✅ Logged in for 24h • Try /balance, /history, /verify, /search" (per D-17). Keep wrong password reply "❌ Wrong password" never echo input. Ensure login rate limit 5/60s via isRateLimited. Replace friendly error strings: wherever "rate limited — slow down" appears use "⏳ Slow down — you hit 30/min, try again in 12s. Tip: use /history 2026-09-01" pattern (per D-14) parametrized by retry hint. Keep buildHelpReply sections + inline but ensure it also serves Welcome context — Welcome owns unauth path.

    Extend src/worker.ts: import registerMenuIfConfigured in start() right after registerTelegramWebhookIfConfigured() call (before watch/poll). Keep single http.createServer on PORT 8080 reuse pattern — do NOT create second server. For POST /telegram/webhook, capture triggering messageId via (body as any).message?.message_id for login delete path: when routing to handleLogin pass messageId through. For unauthenticated non-public commands, reply with buildWelcomeReply() via sendTelegramMessage(chatId,welcome.text,{replyMarkup:welcome.replyMarkup}) instead of bare "Please /login". Ensure verifySecretToken lowercased header check stays layer 1. Keep global 15/10s rate limit but update its reply to friendly D-14 string. Keep answerCallbackQuery existing path but delegate to imported helper.

    No new runtime deps; Node fetch + crypto already present; pino reuse.
  </action>
  <verify>
    <automated>npm run lint 2>&1 | tail -20</automated>
  </verify>
  <done>Single fetch PRIMITIVE extension provides delete/edit/answer helpers; menu.ts registers 7 commands + Menu Button globally on boot; history renders card-per-TX under 4096 with 10/page; /login deletes password message in same tick and never logs it; unauth gets Welcome card no DB touch; friendly errors replace technical strings; worker still single PORT 8080.</done>
</task>

<task type="auto">
  <name>Wire login delete end-to-end + Welcome dispatch + rate-limit friendly strings</name>
  <files>src/worker.ts, src/telegram/commands.ts, src/telegram/webhook.ts</files>
  <read_first>src/worker.ts:302-412 (webhook branch full handler with early session gate reading rawTextEarly earlyCmd publicCmds), src/telegram/commands.ts:50-80 (handleLogin) + 575-607 (handlers map), src/telegram/session.ts:1-61</read_first>
  <action>
    Harden the login delete wiring that the tracer stubbed: In src/worker.ts POST /telegram/webhook handler, after readJsonBody extract update.message?.message_id as maybeNumber; when dispatching, if cmd==='login' capture messageId and ensure handleLogin receives it. Modify handleTelegramUpdate or the handler indirection to accept messageId param: simplest is worker intercepts login before handleTelegramUpdate — check if earlyCmd==='login' then call handleLogin directly with messageId instead of generic handleTelegramUpdate path, to guarantee delete path. If instead plumbing through handleTelegramUpdate, update its signature to forward messageId to handleLogin. Ensure swallow path: deleteTelegramMessage never blocks login success; fire-and-forget.

    Ensure unauth Welcome path in worker: replace every sendTelegramMessage "Please /login" string literal with buildWelcomeReply() import — grep before editing to ensure all 3 sites (worker session gate + commands verify stub + any fallback) use Welcome. For authenticated barrier, verify publicCmds Set still ['login','help','start'] and welcome reply includes inline Login + Help.

    Update all rate/cool-down texts to D-14 friendly: replace "Login cooling down — retry in ~60s" with "⏳ Slow down — login cooling down, retry in ~60s. Tip: try again shortly", replace global "rate limited — slow down" with "⏳ Slow down — you hit 30/min, try again in 12s. Tip: use /history 2026-09-01", replace verify/search/poll/watch cooling strings similarly with emoji + hint but keep command context. Keep isRateLimited calls untouched.

    Add _resetWorkerStateForTests exposure for menu if needed (no state to reset but ensure idempotence).
  </action>
  <verify>
    <automated>npm test -- --run 2>&1 | tail -30</automated>
  </verify>
  <done>Login delete is wired with real message_id from Telegram update, Welcome card is the sole unauth reply with no DB touch, all cool-down texts are friendly per D-14, no "Please /login" literals remain.</done>
</task>

<task type="auto">
  <name>Status card polish + help/Menu consistency + smoke test</name>
  <files>src/telegram/commands.ts, src/worker.ts</files>
  <read_first>src/telegram/commands.ts:210-312 (buildStatusReply sections with emojis statusEmoji staleStr dbEmoji already polished), src/telegram/commands.ts:146-208 (buildHelpReply)</read_first>
  <action>
    Keep buildStatusReply as is per D-02 (research picked "keep polished 01.1 sections" — status already uses 🟢/🟡/⚪, 💾 Database, ⏰ Last TX, 📡 Gmail, code blocks, Africa/Lagos, health link). Only ensure any raw string interpolations escapeHtml and handle missing values gracefully; do not rebuild as chart or one-liner. Ensure Help keeps sections + inline keyboard per D-04: keep 🤖 Admin Console with 📊 Monitoring 💳 Transactions ⚡ Actions sections and inline 📊 Status | 📜 History etc. Verify menu COMMANDS in menu.ts match help command list (help,status,balance,history,search,verify,summary) descriptions aligned.

    Add simple file-owned smoke check: after build, node -e require existence of src/telegram/menu.ts and src/telegram/sendMessage.ts helpers can be imported without throw (no test harness needed).

    Ensure worker start sequence logs "registerMenuIfConfigured" info lines for health (getMyCommands length).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Status retains card-with-sections polish, help retains sections + inline keyboard, Menu Button commands align with /help, worker boot logs menu health without EADDRINUSE.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telegram → POST /telegram/webhook | Untrusted; header secret is only auth; message_id for delete comes from Telegram |
| Telegram text (password, history args) → session/DB | Untrusted; timingSafeEqual, date parse, escapeHtml are controls |
| Postgres transactions → HTML card rendering | Trusted DB but mis-escaped sender would break parse_mode |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Spoofing | POST /telegram/webhook | critical | mitigate | verifySecretToken timingSafeEqual lowercased before body parse — already in worker.ts:309 |
| T-07-02 | Information Disclosure | /login davy123fun123 in chat history | high | mitigate | deleteTelegramMessage(chatId,messageId) best-effort immediately after timingSafeEqual login success per D-17; swallow 400 (>48h) |
| T-07-03 | Information Disclosure | pino logs persisting password via update.message.text | high | mitigate | Extend logger redact remove:true for TELEGRAM_BOT_PASSWORD, TELEGRAM_BOT_TOKEN, TELEGRAM_WEBHOOK_SECRET, OPENROUTER_API_KEY, text, password; never logger.info({text}) |
| T-07-04 | Spoofing | brute force /login | high | mitigate | login 5/60s per-chat isRateLimited, generic "Wrong password" no echo, timingSafeEqual constant-time |
| T-07-05 | Tampering | HTML injection via sender name breaking parse_mode | medium | mitigate | escapeHtml every DB string before <b><code>; fallback attempt(undefined) on "can't parse entities" already in sendMessage |
| T-07-06 | Denial of Service | global bursts on webhook | medium | mitigate | Keep 15/10s global sliding window + per-command limits; always 200 OK no throw; friendly ⏳ hint reduces retries |
| T-07-07 | Tampering | callback_data >64B on history pagination | medium | mitigate | Keep callback_data short "/history YYYY-MM-DD YYYY-MM-DD 10 offset" ≤40B; validate before send |
| T-07-SC | Tampering | npm/pip/cargo installs | high | mitigate | Zero new deps — reuse Node fetch + crypto + pg/pino/zod/date-fns already installed; if any install needed gate on package legitimacy |
</threat_model>

<verification>
- `npm run lint` passes (no EADDRINUSE — single PORT 8080).
- Manual local: mock fetch to capture deleteMessage payload — /login with message_id 123 triggers POST bot/deleteMessage {chat_id, message_id:123} once and login succeeds even if delete returns 400.
- Unauth curl POST /telegram/webhook with valid secret but no session for /balance returns Welcome card (inline Login) and zero DB query in logs.
- Auth then /history with 15 rows renders card-per-TX (💳 👤 📅 🏦 📝 🔖 💰) with no pre table, each DB string escaped, total <4096, Last card not sliced mid-line.
- Boot logs show setMyCommands ok + setChatMenuButton ok + getMyCommands health; Telegram client shows Menu Button with 7 commands.
- Railway logs never contain davy123fun123 after login attempt (redacted).
</verification>

<success_criteria>
- deleteTelegramMessage helper calls bot/deleteMessage with chat_id+message_id and swallows 400/429 (verifiable via fetch spy).
- Welcome card is sole unauth reply with inline Login no DB touch.
- History output is card-per-transaction per D-01 under 4096, 10/page, cleaned sender via extractSender.
- Menu registration calls both setMyCommands and setChatMenuButton globally on boot idempotently.
- Friendly error strings replace all technical rate-limit texts per D-14.
</success_criteria>

<output>
Create `.planning/phases/07-ux-overhaul/07-01-SUMMARY.md` when done
</output>
