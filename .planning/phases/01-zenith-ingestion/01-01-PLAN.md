---
phase: 01-zenith-ingestion
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - vitest.config.ts
  - .env.example
  - railway.json
  - Dockerfile
  - src/config/env.ts
  - src/db/pool.ts
  - src/db/migrate.ts
  - migrations/001_transactions.sql
  - migrations/002_suspicious_emails.sql
  - migrations/003_pipeline_health.sql
  - src/observability/logger.ts
  - src/gmail/auth.ts
  - src/gmail/fetch.ts
  - src/zenith/authenticity.ts
  - src/zenith/decode.ts
  - src/zenith/parser.ts
  - src/zenith/validation.ts
  - src/zenith/classifier.ts
  - src/db/transactions.ts
  - src/db/suspicious.ts
  - src/db/health.ts
  - src/worker.ts
  - src/index.ts
  - tests/fixtures/zenith-credit-sample.eml
  - tests/tracer.test.ts
requirements:
  - FR-1.1
  - FR-1.4
  - FR-1.6
  - FR-1.7
  - FR-1.8
  - FR-1.11
  - FR-1.12
  - NFR-1.2
  - NFR-1.3
  - NFR-1.5
  - NFR-1.6
autonomous: true
estimate:
  tokens: 62000
  raw_tokens: 31000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - A single production-quality processEmail(messageId) path can take a Gmail message ID through auth check, strict B64->QP->HTML decode, cheerio field extraction, credit classification, zod validation, deduped atomic insert + heartbeat, and JSON logging end-to-end (FR-1.4/1.6/1.7/1.8/1.11/1.12)
    - Postgres schema exists on Railway with email_message_id UNIQUE enforcing dedup and pipeline_health single-row heartbeat (FR-1.8, FR-1.11, NFR-1.3)
    - Gmail OAuth authenticates with personal refresh_token in-memory refresh only, no DB persistence of rotated access tokens (D-01)
  artifacts:
    - package.json with pinned deps googleapis@178.1.1 pg@8.23.0 pino@10.3.1 cheerio@1.2.0 zod@4.5.4 quoted-printable@1.0.1
    - migrations/001_transactions.sql, 002_suspicious_emails.sql, 003_pipeline_health.sql applied via src/db/migrate.ts
    - src/gmail/auth.ts OAuth2Client factory with setCredentials({refresh_token}) only
    - src/zenith/authenticity.ts verifyAuthenticity() DKIM-only clause-bound check
    - src/worker.ts single convergence processEmail() wired end-to-end
  key_links:
    - processEmail -> verifyAuthenticity -> decodeStrict -> parseZenithFields -> validateTransaction -> insertTransactionAtomically + heartbeat TX
    - gmail auth -> gmail API -> fetch -> processEmail -> pg Pool with ssl:{rejectUnauthorized:false}
    - pino logger child(email_message_id) threaded through every stage to Railway stdout
---

<objective>
Tracer slice: prove the architecture end-to-end before expansion. One Railway project + Postgres + migrations + Gmail OAuth connection + single processEmail path verified against a real Zenith fixture from zenith_bank_email_format.md. Per D-05/D-09/D-07/D-06 this tracer uses the strict documented B64->QP->HTML pipeline, DKIM-only clause-bound verification, and credit-only classification — no fallbacks, no debit path.

Purpose: Catch architectural dead-ends (SSL/Railway networking, token refresh, mime normalization, DB constraint races) on the best context tokens before horizontal expansion.
Output: Committed skeleton that can already insert one correct transactions row per verified Zenith credit alert fixture and dedup it on replay.
</objective>

<execution_context>
@C:/Users/user/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/user/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/plan.md
@.planning/phase-1-zenith-ingestion-plan.md
@.planning/phases/01-zenith-ingestion/01-CONTEXT.md
@.planning/phases/01-zenith-ingestion/01-RESEARCH.md
@zenith_bank_email_format.md
@envExample.example
</context>

<tasks>

<task type="auto">
  <name>Task 1.1: Project scaffold + Railway infra + Postgres + migrations</name>
  <reversibility rating="one-way">Fresh Railway project payment-verification + Postgres schema (transactions/suspicious_emails/pipeline_health) — downstream phases read transactions; changing columns or constraints after Phase 2 requires coordinated migration.</reversibility>
  <files>package.json, tsconfig.json, vitest.config.ts, .env.example, railway.json, Dockerfile, src/config/env.ts, src/db/pool.ts, src/db/migrate.ts, migrations/001_transactions.sql, migrations/002_suspicious_emails.sql, migrations/003_pipeline_health.sql, src/observability/logger.ts</files>
  <read_first>
    - .planning/phase-1-zenith-ingestion-plan.md Section 6 Data Model (exact columns, which UNIQUE to enforce now vs defer transaction_reference UNIQUE)
    - .planning/phases/01-zenith-ingestion/01-RESEARCH.md Postgres Pool — Railway Internal Networking pitfalls (ssl:{rejectUnauthorized:false} via config object not query string)
    - .planning/phases/01-zenith-ingestion/01-RESEARCH.md System Architecture Diagram + Recommended Project Structure
    - envExample.example Phase 1 block (var names to validate)
    - .planning/plan.md Section 5.4 / 5.4.1 / 5.5 and Section 21 env vars
  </read_first>
  <action>
    Scaffold Node 20 LTS + TypeScript project per RESEARCH Standard Stack (per D-11 ease/troubleshooting/performance/Railway simplicity — using Node+TS per D-11, not JS): npm init, install googleapis@178.1.1 google-auth-library@11.0.2 pg@8.23.0 pino@10.3.1 cheerio@1.2.0 zod@4.5.4 quoted-printable@1.0.1 date-fns@4.4.0 date-fns-tz@3.2.0 and dev deps typescript@5.6 tsx@4.7 @types/node@20 @types/pg@8.10 @types/quoted-printable vitest@3. Create tsconfig.json (ES2022, module commonjs or NodeNext with tsx, strict true, outDir dist, rootDir src). Create src/config/env.ts that uses zod to validate every Phase 1 env var from envExample.example (DATABASE_URL, GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET, GOOGLE_REFRESH_TOKEN, GOOGLE_CLOUD_PROJECT, GOOGLE_PUBSUB_TOPIC, GOOGLE_PUBSUB_SUBSCRIPTION, ZENITH_SENDER_DOMAINS, ALERT_WEBHOOK_URL, STALENESS_THRESHOLD_MINUTES, BUSINESS_HOURS_*, POLL_INTERVAL_MINUTES) — ZENITH_SENDER_DOMAINS is comma-separated split+trim with default zenithbank.com, numeric thresholds parsed as numbers, missing required vars throw at boot per NFR-1.6. Create src/db/pool.ts as pg.Pool singleton with connectionString=DATABASE_URL and ssl:{rejectUnauthorized:false} (must be config object, never ?sslmode= query param per Pitfall 3), max 10, idleTimeoutMillis 30000, plus pool.on('error') handler to prevent process crash. Create src/observability/logger.ts exporting pino logger with level from LOG_LEVEL env default info, formatters to include email_message_id binding via child logger, JSON to stdout only per D-14 (no file transport). Create migrations: 001_transactions.sql — id UUID PK default gen_random_uuid(), amount NUMERIC(14,2) NOT NULL CHECK (amount > 0), currency CHAR(3) NOT NULL, transaction_reference TEXT, transaction_date DATE NOT NULL, transaction_time TIME, sender_name TEXT, sender_account TEXT (store masked as-is per D-08), description TEXT, branch TEXT, available_balance NUMERIC(14,2), bank TEXT NOT NULL DEFAULT 'Zenith Bank', email_message_id TEXT NOT NULL UNIQUE, email_auth_result TEXT NOT NULL, raw_email TEXT, matched_receipt_id UUID NULL, matched_at TIMESTAMPTZ NULL, created_at TIMESTAMPTZ NOT NULL DEFAULT now() — do NOT add UNIQUE on transaction_reference yet per Section 6 note; add pgcrypto extension if needed for gen_random_uuid. 002_suspicious_emails.sql — id UUID PK, email_message_id TEXT UNIQUE, from_address TEXT NOT NULL, subject TEXT, auth_result TEXT NOT NULL, reason TEXT NOT NULL, raw_email TEXT, created_at TIMESTAMPTZ NOT NULL DEFAULT now(). 003_pipeline_health.sql — key TEXT PK, value TIMESTAMPTZ, updated_at TIMESTAMPTZ NOT NULL DEFAULT now(); seed row ('last_zenith_email_processed_at', null). Create src/db/migrate.ts that reads migrations/*.sql in lexical order, tracks applied in a migrations table (CREATE TABLE IF NOT EXISTS migrations ...), runs each unapplied file in a transaction, logs via pino. Create railway.json with buildCommand npm ci && npm run build or tsx start, startCommand node --enable-source-maps dist/worker.js or npx tsx src/worker.ts (prefer tsx for Phase 1 to avoid build step per D-11, document chosen), healthcheck off for worker, and reference variable DATABASE_URL=${{Postgres.DATABASE_URL}} noted in README comment. Create Dockerfile with node:20-slim, npm ci, copy, non-root user. Create .env.example copying envExample.example Phase 1 block. Per D-12 fresh Railway project payment-verification with PostgreSQL + single worker service, no API service yet, secrets via Railway env vars only (NFR-1.2) — document manual Railway setup steps in a comment header of railway.json (project create, postgres plugin, variable reference). This task does NOT write Gmail topic/subscription infrastructure — that is 01-03.
  </action>
  <verify>
    <automated>npm ci; npx tsc --noEmit; npm run migrate 2>&1 | head -n 50; node -e "require('./src/config/env.ts')" 2>&1 || npx tsx -e "import './src/config/env.ts'" 2>&1; psql "$DATABASE_URL" -c "\d transactions" | grep -q email_message_id && echo "schema ok"</automated>
  </verify>
  <acceptance_criteria>
    - [ ] npx tsc --noEmit passes with strict true and no any-typed pool/logger leaks
    - [ ] migrations run idempotently; \d transactions shows email_message_id UNIQUE, amount check, pipeline_health key PK; rerunning migrate exits 0 with 0 new files
    - [ ] src/config/env.ts throws on missing DATABASE_URL or GOOGLE_REFRESH_TOKEN and correctly splits ZENITH_SENDER_DOMAINS="a.com, b.com" into ["a.com","b.com"]
    - [ ] src/db/pool.ts uses ssl:{rejectUnauthorized:false} object (grep -v '^#' src/db/pool.ts | grep -q "rejectUnauthorized.*false") and has pool.on('error')
    - [ ] src/observability/logger.ts emits JSON (node -e "import pino..." produces {"level":30} line), no console.log string concatenation
  </acceptance_criteria>
  <done>Project builds cleanly, env validated via zod, pg Pool configured for Railway internal networking, three migrations applied, pino JSON logger factory ready, Railway scaffolding documented.</done>
</task>

<task type="auto">
  <name>Tracer: End-to-end CREDIT TRANSACTION NOTIFICATION through processEmail — one happy path only</name>
  <files>src/gmail/auth.ts, src/gmail/fetch.ts, src/zenith/authenticity.ts, src/zenith/decode.ts, src/zenith/parser.ts, src/zenith/classifier.ts, src/zenith/validation.ts, src/db/transactions.ts, src/db/suspicious.ts, src/db/health.ts, src/worker.ts, tests/fixtures/zenith-credit-sample.eml, tests/tracer.test.ts</files>
  <read_first>
    - zenith_bank_email_format.md Body Encoding pipeline (B64->QP->HTML), Full Crediting Example HTML table, Field Regex table, Description families extraction rules
    - .planning/phases/01-zenith-ingestion/01-RESEARCH.md Code Examples: Gmail OAuth2 Client — Personal Refresh Token, Strict Base64->QP->HTML Decoding, Cheerio Table Extraction, DKIM-Only Authenticity Verification, Atomic Insert+Heartbeat+Dedup, Common Pitfalls 2/4/6
    - .planning/phase-1-zenith-ingestion-plan.md Section 4 FR-1.3 through FR-1.9, Section 6 Data Model column list, Section 7.2 single convergence processEmail
  </read_first>
  <action>
    Implement the single convergence function processEmail(messageId: string, deps: { gmail, pool, logger }): Promise<'inserted'|'duplicate'|'suspicious'|'ignored'|'validation_failed'> that is called by both push and poll (per RESEARCH Pattern 1 and DRY boundary). Wire steps in exact order: 1) fetch via src/gmail/fetch.ts helper that calls gmail.users.messages.get({userId:'me', id:messageId, format:'full'}) and returns headers map (lowercased names) plus raw payload.parts body data; include base64url normalization helper base64UrlToBase64(s) (-_→+/ + pad to 4) before Buffer.from. 2) Call src/zenith/authenticity.ts verifyAuthenticity(authResultsHeader:string) per D-07 strict clause-bound regex: split header on ';', iterate clauses where /^\s*dkim\s*=\s*pass\b/i, extract header.d via /header\.d\s*=\s*([^\s;]+)/i else fallback /\bd\s*=\s*([^\s;]+)/i or header.i, lowercase, require ==='zenithbank.com'; return {pass,domain,reason}. If missing header fail closed (pass:false). 3) Candidate filter: check From header domain ∈ ZENITH_SENDER_DOMAINS split list before authenticity — if not candidate, return 'ignored' with debug log only (no suspicious, no alert). 4) If auth fail → insert into suspicious_emails via src/db/suspicious.ts ON CONFLICT DO NOTHING + alert hook stub (defer full Telegram to 01-04, but call alerter.suspicious with no-op if not wired) and return 'suspicious'. 5) Decode: implement src/zenith/decode.ts decodeStrict(rawBodyB64:string):string strictly Buffer.from(normalizedB64,'base64').toString('utf-8') → quotedPrintable.decode(qpString) with no fallback per D-09; throw on base64 or QP failure so caller treats as parse failure (will be alerted in 01-04, for tracer just throw). 6) Parse: src/zenith/parser.ts parseZenithFields(html:string) using cheerio per RESEARCH — cheerio.load, $('table tr'), cells[0] label lowercased trimmed without trailing colon, cells[1] value; return Record&lt;string,string&gt; for keys account number/date of transaction/amount/currency/description/reference code/branch/transaction type/available balance. 7) Classifier: src/zenith/classifier.ts isCreditTransaction(fields, rawSubject:string):boolean — require CREDIT TRANSACTION NOTIFICATION in subject OR Transaction Type === 'Credit' (case-insensitive); if DEBIT TRANSACTION NOTIFICATION or Type Debit → return false and caller returns 'ignored' at debug per D-06. 8) Validation: src/zenith/validation.ts uses zod per FR-1.7 — amount: string with regex ^[\d,]+\.\d{2}$ then transform Number(v.replace(/,/g,'')) >0 max 2 decimals, currency enum ['NGN','USD','EUR','GBP'], transaction_reference non-empty, transaction_date parsed via date-fns parse(value,'dd/MM/yyyy',new Date()) must beValid and not future beyond 5 min skew, sender_account non-empty. Throw or return validation errors; caller maps to 'validation_failed'. 9) Persistence: src/db/transactions.ts insertTransactionAtomically(row):Promise<'inserted'|'duplicate'> must run in single TX: BEGIN, INSERT INTO transactions ... ON CONFLICT (email_message_id) DO NOTHING RETURNING id, if rowCount 0 ROLLBACK return duplicate, else INSERT pipeline_health key last_zenith_email_processed_at ON CONFLICT DO UPDATE SET value=EXCLUDED.value, then COMMIT. Also implement src/db/health.ts get/set helpers. 10) Handle raw_email capping per D-17/NFR-1.4: function stripAndCap(msg):string that strips CID/inline image parts (detect parts with Content-Type image/* or Content-ID) and enforces Buffer.byteLength ≤100*1024, truncating with marker if larger. Store capped string. 11) Logging: every stage emits via logger.child({email_message_id:messageId}) at appropriate level per D-14 (received, auth result with reason, parse fields, validation, dedup result, insert result). Implement src/gmail/auth.ts per RESEARCH: new OAuth2(CLIENT_ID,CLIENT_SECRET,'urn:ietf:wg:oauth:2.0:oob'), setCredentials({refresh_token: GOOGLE_REFRESH_TOKEN}) ONLY (no access_token, so library fetches fresh), optional eagerRefreshThresholdMillis 300000, on('tokens') only logs debug and warns if refresh_token rotated. Export gmail = google.gmail({version:'v1', auth: oauth2}). Create tests/fixtures/zenith-credit-sample.eml as a reconstructed fixture: take the Full Crediting Example HTML from zenith_bank_email_format.md, then encode inner QP manually (e.g. use quoted-printable encode) then outer base64url, and craft headers {Authentication-Results: 'mx.google.com; dkim=pass header.d=zenithbank.com header.s=selector...; spf=pass', From: 'alerts@zenithbank.com', Subject: 'CREDIT TRANSACTION NOTIFICATION', 'Message-Id': '<tracer-test-001@zenithbank.com>'} — ensure decodeStrict round-trips to HTML containing Account Number row. Create tests/tracer.test.ts that mocks gmail.users.messages.get to return that fixture for a fake messageId 'tracer-001', calls processEmail('tracer-001'), asserts DB transactions row exists with amount 10000.00 currency NGN description CIP CR/... sender_name extracted SAMPLE ACCOUNT HOLDER per extraction rule (text between CIP CR/ and first /), transaction_reference ZIB20260908123456, sender_account 999****999 stored masked as-is per D-08, bank Zenith Bank, pipeline_health updated, second call with same messageId returns 'duplicate' and does not create second row. Wire src/worker.ts minimal entry that init pool, logger, gmail, and exports processEmail for tests (full timers/push/poll deferred to 01-03).
  </action>
  <verify>
    <automated>npm test -- tests/tracer.test.ts --run 2>&1 | tail -n 40
psql "$DATABASE_URL" -c "select amount,currency,transaction_reference,sender_name,sender_account,branch from transactions where email_message_id='tracer-001';"</automated>
  </verify>
  <acceptance_criteria>
    - [ ] tests/tracer.test.ts green: one CREDIT row inserted with exact fields from Full Crediting Example (amount 10000.00, currency NGN, sender SAMPLE ACCOUNT HOLDER, masked account untouched), pipeline_health.last_zenith_email_processed_at is now within 60s
    - [ ] Second processEmail('tracer-001') returns 'duplicate' and SELECT count(*) where email_message_id='tracer-001' is 1 (ON CONFLICT DO NOTHING proven)
    - [ ] verifyAuthenticity rejects a fixture where Authentication-Results has dkim=pass d=attacker.com (attacker domain with valid DKIM) — routed to suspicious_emails not transactions
    - [ ] decodeStrict throws on non-base64 input (no silent fallback) per D-09
    - [ ] All log lines in test output contain "email_message_id":"tracer-001" correlation key (grep pino JSON)
  </acceptance_criteria>
  <done>Single happy path works end-to-end and is committed: Gmail fetch (mocked) → DKIM clause-bound pass with d=zenithbank.com → strict B64→QP→HTML → cheerio table → credit classifier → zod validation → deduped atomic TX + heartbeat → pino JSON log. Duplicate replay is idempotent.</done>
</task>

<task type="auto">
  <name>Task 1.3: DB helpers consolidation + worker entry + idempotent migration runner wiring</name>
  <files>src/db/health.ts, src/db/transactions.ts, src/db/suspicious.ts, src/worker.ts, src/index.ts, tests/unit/authenticity.test.ts</files>
  <read_first>
    - .planning/phases/01-zenith-ingestion/01-RESEARCH.md Pitfalls 8 (raw_email bloat), 3 (Railway SSL), and Pattern 4 atomic heartbeat
    - .planning/phase-1-zenith-ingestion-plan.md Section 10.1 T-1.1 through T-1.5 (unit expectations for verifyAuthenticity)
  </read_first>
  <action>
    Consolidate remaining DB helpers not covered by tracer skeleton: finalize src/db/health.ts with getHealth(key):Promise<string|null> and setHealth(key,value):Promise<void> using INSERT ... ON CONFLICT DO UPDATE, plus getLastProcessedAt():Promise<Date|null> reading value as TIMESTAMPTZ. Finalize src/db/suspicious.ts insertSuspicious(row):Promise<'inserted'|'duplicate'> with ON CONFLICT (email_message_id) DO NOTHING and stripAndCap for raw_email. Harden src/db/transactions.ts column mapping to match Section 6 types (NUMERIC handling via string or number per pg). Wire src/worker.ts startup sequence for Phase 1: load env.ts first (fail fast), create logger, create pool (connect test query SELECT 1), run migrate.ts, create gmail client from auth.ts, expose processEmail export and a start() that will later add timers/push (no timers yet). Create src/index.ts barrel re-exports for tests. Add tests/unit/authenticity.test.ts covering T-1.1 to T-1.5: dkim=pass+zenithbank.com pass, dkim=pass+attacker.com fail, dkim=fail fail, missing header fail closed, display-name spoof with From Zenith Bank but non-Zenith d fail — all via pure verifyAuthenticity without Gmail. Ensure coverage of semicolon clause binding (multiple DKIM signatures case). Keep coverage table in test comments referencing T-IDs.
  </action>
  <verify>
    <automated>npm run migrate 2>&1 | tail -n 20
npm test -- tests/unit/authenticity.test.ts --run 2>&1 | tail -n 30</automated>
  </verify>
  <acceptance_criteria>
    - [ ] migrations table shows 3 rows, health helpers round-trip (setHealth then getHealth returns same TIMESTAMPTZ within 2s)
    - [ ] tests/unit/authenticity.test.ts 5 cases green, including the multi-signature attacker-domain case that binds dkim=pass to its own clause
    - [ ] npm run build or npx tsx smoke: node -e "import('./src/worker.ts')" exits without pool error when DATABASE_URL unset? Actually env.ts should throw — verify error message names missing var per NFR-1.6
  </acceptance_criteria>
  <done>DB helpers complete and tested, migration runner idempotent, worker entry boots and exports processEmail for downstream plans, authenticity unit coverage proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Gmail API → Worker | Untrusted email bodies + headers arrive here; DKIM verification is the trust gate |
| Worker → Postgres | Parameterized queries only; UNIQUE constraint is security control for dedup replay |
| Env / Railway → Worker | Secrets boundary; no credential in repo |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Spoofing | src/zenith/authenticity.ts | critical | mitigate | DKIM-only clause-bound check: require dkim=pass AND header.d=zenithbank.com in SAME semicolon clause per RFC 8601; fail closed on missing header; unit tests T-1.2/T-1.5 prove attacker-domain rejection; never check display name |
| T-01-02 | Tampering | src/zenith/decode.ts + parser | high | mitigate | Strict B64->QP->HTML pipeline with no fallback per D-09; throw on bad base64/QP so tampered encoding never silently yields partial object; cheerio table extraction not regex on raw HTML |
| T-01-03 | Repudiation | src/observability/logger.ts | medium | mitigate | pino JSON child logger with email_message_id correlation on every stage; email_auth_result stored on row for traceability |
| T-01-04 | Information Disclosure | src/config/env.ts + src/db/pool.ts | high | mitigate | Secrets only via Railway env vars per NFR-1.2; pool uses private DATABASE_URL reference variable; never log tokens; logger redacts auth headers |
| T-01-05 | Denial of Service | src/db/transactions.ts | medium | mitigate | ON CONFLICT DO NOTHING + idempotent processEmail prevents duplicate-spam amplification; pool max 10 prevents connection exhaustion |
| T-01-06 | Elevation | Postgres | medium | mitigate | Parameterized queries only (pg placeholders $1...); no string-concatenated SQL; least-privilege Railway DB user |
| T-01-SC | Tampering | npm installs | high | mitigate | Package Legitimacy Audit in RESEARCH passed for googleapis/pg/pino/cheerio/zod/quoted-printable; verify npm registry before install; no postinstall scripts |
</threat_model>

<verification>
- Tracer fixture CREDIT email round-trips through decodeStrict and inserts exactly one transactions row with correct amount/currency/sender/reference and masked account as-is per D-08
- Duplicate replay returns duplicate and does not create second row (security control T-4.3)
- Attacker DKIM domain fixture lands in suspicious_emails not transactions and would fire alert path
- Non-credit (DEBIT) fixture is ignored at debug with no row in either table per D-06
- npm run migrate idempotent; npx tsc --noEmit clean; pino stdout is JSON not strings
</verification>

<success_criteria>
- One real Zenith-format credit alert fixture produces one correct deduplicated transactions row within the tracer test, heartbeat advances, spoofed DKIM fails closed, and every pipeline stage logs with email_message_id.
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces (Plan 01-01 tracer scope)

**Files created by this plan:**
- package.json — deps: googleapis@178.1.1, google-auth-library@11.0.2, pg@8.23.0, pino@10.3.1, cheerio@1.2.0, zod@4.5.4, quoted-printable@1.0.1, date-fns@4.4.0, date-fns-tz@3.2.0; dev: typescript@5.6, tsx@4.7, @types/node@20, @types/pg, vitest@3
- tsconfig.json — ES2022 strict
- .env.example — Phase 1 vars mirror of envExample.example
- railway.json / Dockerfile — Railway payment-verification project scaffolding (worker service + Postgres reference variable DATABASE_URL)
- src/config/env.ts — zod-validated env loader (ZENITH_SENDER_DOMAINS split, thresholds, business hours); exports env
- src/db/pool.ts — pg.Pool singleton with ssl:{rejectUnauthorized:false} + error handler; exports pool
- src/db/migrate.ts — versioned migration runner against migrations table; exports runMigrations()
- migrations/001_transactions.sql / 002_suspicious_emails.sql / 003_pipeline_health.sql — DDL + seed
- src/observability/logger.ts — pino JSON logger factory + child(email_message_id) helper; exports logger, createChildLogger
- src/gmail/auth.ts — OAuth2Client with setCredentials({refresh_token}) only; exports gmail, oauth2, getGmailClient
- src/gmail/fetch.ts — getMessageFull() with base64url normalization; exports fetchMessage, base64UrlToBase64
- src/zenith/authenticity.ts — verifyAuthenticity(header:string):{pass,domain,reason} DKIM-only clause-bound
- src/zenith/decode.ts — base64UrlToBase64 + decodeStrict(b64):string strict B64->QP->HTML
- src/zenith/parser.ts — parseZenithFields(html):Record plus parseZenithEmail wrapper
- src/zenith/classifier.ts — isCreditTransaction / isTransactionAlert (minimal in tracer)
- src/zenith/validation.ts — zod schema + validateTransaction (minimal in tracer, full in 01-02)
- src/db/transactions.ts — insertTransactionAtomically(row) with ON CONFLICT + heartbeat TX
- src/db/suspicious.ts — insertSuspicious(row)
- src/db/health.ts — getHealth/setHealth, getLastProcessedAt, getHistoryId/setHistoryId
- src/worker.ts — start() + processEmail(messageId) single convergence export
- src/index.ts — barrel
- tests/fixtures/zenith-credit-sample.eml — encoded fixture from zenith_bank_email_format.md Full Example
- tests/tracer.test.ts — end-to-end tracer assertion

**Symbols exported for later phases / pattern mapper:**
- processEmail(messageId:string):Promise<'inserted'|'duplicate'|'suspicious'|'ignored'>
- verifyAuthenticity, decodeStrict, parseZenithFields, validateTransaction
- insertTransactionAtomically, insertSuspicious, getLastProcessedAt, pool, logger, gmail
- runMigrations(), env
</artifacts_this_phase_produces>

<api_coverage>
## API Coverage — Plan 01-01

| External API | Operation | Library | Plan coverage |
|--------------|-----------|---------|---------------|
| Gmail API | users.messages.get(format=full) | googleapis@178.1.1 | 01-01 tracer via src/gmail/fetch.ts; mocked in tracer test, real call in 01-03 |
| Gmail API | OAuth token refresh (OAuth2Client) | google-auth-library@11.0.2 | 01-01 src/gmail/auth.ts in-memory only per D-01 |
| Telegram Bot API | POST /bot{token}/sendMessage | fetch (native) | Stub hook in tracer suspicious path; full implementation in 01-04 |
| Postgres | DDL + INSERT ON CONFLICT + heartbeat TX | pg@8.23.0 | 01-01 migrations + atomic insert |
</api_coverage>

<schema_push>
## Schema Push — Plan 01-01
This plan creates and pushes the Phase 1 schema. Migrations run via src/db/migrate.ts (pre-deploy command) against Railway Postgres DATABASE_URL. Migration table tracks applied files idempotently; rerunning never replays DDL. Phase 1 owns transactions, suspicious_emails, pipeline_health — no other service may alter these without a new migration file.
</schema_push>

<output>
Create .planning/phases/01-zenith-ingestion/01-01-SUMMARY.md when done
</output>
