# Phase 01: Zenith Ingestion — Verification Report

**Phase goal (01-CONTEXT.md):** Trustworthy, continuously-monitored `transactions` ledger populated exclusively from genuine Zenith Bank transaction alert emails. No WhatsApp/OCR/matching — just one correct `transactions` row per verified Zenith credit alert within latency budget, spoofed never reaches ledger, duplicates never create extra rows, pipeline silence/breakage surfaced automatically.

**Verified:** 2026-09-09T14:50:00+01:00
**Status:** PASS
**Score:** 7/7 exit criteria verified

---

## Exit Criteria (Section 3 of phase-1-zenith-ingestion-plan.md)

| # | Criterion | Verdict | Evidence |
|---|-----------|---------|----------|
| 1 | Every valid authenticity-verified Zenith credit alert becomes one correct `transactions` row | **PASS** | `src/worker.ts:processEmail` single convergence path: candidate From filter → `verifyAuthenticity` → `decodeStrict` → `parseZenithEmail` (cheerio per-field) → `isCreditTransaction` → `extractSender` → `validateTransaction` → `insertTransactionAtomically` (BEGIN/COMMIT + heartbeat). `src/db/transactions.ts:60-65` ON CONFLICT DO NOTHING + heartbeat only on COMMIT. Tests: `tracer.test.ts` inserts `amount 10000.00 NGN SAMPLE ACCOUNT HOLDER 999****999 ZIB20260908123456` + heartbeat within 60s; `pipeline.test.ts T-2.1` credit+NIP both insert + heartbeat UTC; `monitoring.test.ts` confirms heartbeat advances on COMMIT literal `now()`. `npm test` shows inserted logs. |
| 2 | Spoofed (wrong DKIM domain) never reaches `transactions`, lands in `suspicious_emails` with alert | **PASS** | `src/zenith/authenticity.ts:13-45` DKIM-only clause-bound: splits `Authentication-Results` on `;`, requires `dkim=pass` per clause, extracts `header.d`/`d`/`header.i`, lowercases, requires `=== 'zenithbank.com'`, fail-closed on missing. Wrong domain returns `dkim=pass but d!=zenithbank.com`. `src/worker.ts:82-101` fail → `insertSuspicious` (ON CONFLICT dedup) + `alerter.suspicious` (`Possible spoof: ... email_message_id=`). `src/alerts/alerter.ts:93-96` `Possible spoof` prefix vs drift; `src/db/suspicious.ts` dedup. Tests: `tracer.test.ts` attacker DKIM domain → suspicious, `pipeline.test.ts T-2.2`, `resilience.test.ts T-4.1/T-4.2` no-DKIM and attacker-domain both suspicious, `monitoring.test.ts T-6.4` bodies contain `Possible spoof` not `format drift`. Anti-tamper parse for typosquat `zenithbanks.com` ignored before auth. |
| 3 | Same email processed twice never duplicates | **PASS** | `migrations/001_transactions.sql:17` `email_message_id TEXT NOT NULL UNIQUE`; `migrations/002_suspicious_emails.sql` UNIQUE; `src/db/transactions.ts:60-87` `ON CONFLICT (email_message_id) DO NOTHING RETURNING id` → `rowCount===0` → ROLLBACK duplicate, no heartbeat update. Heartbeat only on `rowCount>0` COMMIT. Tests: `tracer dup-heartbeat-001`, `pipeline.test.ts T-2.4` duplicate idempotent + per-page checkpoint, `gmail-ingestion.test.ts` poll+push same `shared-id` size stays 2, `resilience T-4.3` replay `r43-replay` inserted→duplicate, `staleness.test.ts` duplicate does NOT update heartbeat. Crash/restart safe via DB constraint not app check (NFR-1.3). |
| 4 | Zenith-domain email that fails parsing triggers distinct alert | **PASS** | `src/zenith/parser.ts:38-45` `ParseFailure` thrown on missing/truncated table (field-named), not partial object. `src/worker.ts:128-147` `ParseFailure` → `alerter.parseFailure({field, rawSubject})` → `validation_failed` return, distinct from `suspicious` and `ignored`. `src/alerts/alerter.ts:98-102` prefix `Zenith format drift: failed to parse {field} subject=… email_message_id=` vs `Possible spoof`. Tests: `monitoring.test.ts T-6.3` truncated HTML → `validation_failed` + body contains `Zenith format drift` + messageId; `pipeline.test.ts T-2.3` parse-fail vs non-Zenith ignored (T-1.9); `validation.test.ts` missing reference, negative amount distinct. `isTransactionAlert` heuristic prevents OTP storm misclassification. |
| 5 | Staleness fires after 60 min no insert during 07:00–21:00 Africa/Lagos, every day, once per window | **PASS** | `src/observability/staleness.ts:6-87` `isBusinessHours` via `formatInTimeZone(tz, 'HH:mm')` lexicographic `HH:mm >=07:00 && <=21:00`, tz defaults `Africa/Lagos`, no weekday check → every day. `parseThresholdMinutes` defaults 60. `checkStaleness` returns `never_seeded` (no fetch) if no heartbeat, `outside_hours` if not in window, `ok` if `elapsed <=60m`, `stale` if `elapsed>60m` → `sendOnce('staleness', text, 60*60*1000)` + local `lastStalenessFiredAt` double guard (60m cooldown). `startStalenessChecker(60_000)` pinned 60s interval unref + immediate tick + isRunning guard. `src/worker.ts:330-333` wires it unref + SIGTERM drains. `src/alerts/alerter.ts:104-107` `Pipeline stale: no Zenith credit in {m}m during business hours` with alerter 60m cooldown. Tests: `staleness.test.ts` 90m at 10:00 WAT → stale once + cooldown holds second call 1 fetch, 22:00 WAT → outside_hours no fetch, never_seeded no fetch, 30m → ok, after 61m fires again (2nd window), WAT mapping 08:00Z=09:00 inside, 21:00Z=22:00 outside, 06:00Z=07:00 inclusive, 20:00Z=21:00 inclusive; `monitoring.test.ts T-6.1` fires once not spamming + T-6.2 new credit resets to ok; interval pinned `unref`/overlap guard tested. |
| 6 | Full test suite passes — 134 passed | **PASS** | `npm test` executed 2026-09-09: **Test Files 15 passed (15), Tests 134 passed (134), Duration 23.31s**. Breakdown: decode 8, parser 14, sender 13, classifier 16, validation 18, authenticity 8, watch 5, poll 4, staleness 7, alerter 5, tracer 7, pipeline 11, gmail-ingestion 6, monitoring 5, resilience 7 = 134. `npx tsc --noEmit` clean per all 4 summaries. Evidence captured in terminal log (tail -80). |
| 7 | No unexplained gaps/duplicates after crash/restart (ON CONFLICT, history 404 fallback) | **PASS** | `ON CONFLICT` above + `src/gmail/watch.ts:43-104` `registerWatch` persists `gmail_history_id` + `gmail_watch_expiration` via `health.ts` TEXT column (migration 004), topic `projects/${GOOGLE_CLOUD_PROJECT}/topics/${GOOGLE_PUBSUB_TOPIC}`, 24h unref renewal, 401 `invalid_grant` caught → `sendOnce('watch-renewal',1h)` no crash. `src/gmail/push-handler.ts:71-149` decodes base64 envelope, validates missing data→400, uses stored `gmail_history_id` as `startHistoryId` never notification `historyId`, drives `fetchHistorySince` pagination, advances cursor only after successful batch, 404/`historyId notFound` → fallback `pollSweep()` + still ACK 200 (no Pub/Sub redelivery storm), batch throw → 500 and cursor NOT advanced (retry same window). `src/gmail/history.ts:27-61` `fetchHistorySince` history.list `messageAdded` pagination. `src/gmail/poll.ts:47-135` independent 15-min `pollSweep` (D-02 safety net) `pollRunning` overlap guard, `q` `(from:zenithbank.com) after:UNIX` 7-day fallback, paginate `maxResults 50` until `nextPageToken`, checkpoint `gmail_poll_after` per page via `setPollAfterMs(Date.now())`, 429 exponential backoff+jitter 3 retries for list. `src/gmail/fetch.ts` `getMessageFull` 429 backoff 3 retries. `src/db/transactions.ts:36-48` `pool.connect` retry 2×100ms for T-5.2. `src/worker.ts` HTTP `/gmail/pubsub`+`/health`, all timers unref, SIGTERM/SIGINT drains staleness/poll/watch timers + http + pool. Tests: `gmail-ingestion.test.ts` 6 tests (cursor, 404 fallback still 200, cursor-not-advanced, poll safety net dedup), `resilience.test.ts T-5.1` kill-mid-batch 5→5 idempotent, `T-5.2` PG retry, `T-5.3` 429 backoff, `T-3.4` same-amount diff refs both stored. |

---

## Spot-Check Results (requested files)

| File | Check | Result |
|------|-------|--------|
| `src/zenith/authenticity.ts` | Clause-bound DKIM-only, fail-closed, `header.d` exact `zenithbank.com` | PASS — 45 LOC, pure function, no SPF, no fallback |
| `src/zenith/decode.ts` | Strict B64→QP→HTML, base64url `-`/`_`+padding normalize, regex invalid chars → throw `strict-decode: base64 fail`, Buffer round-trip, quoted-printable decode | PASS — no silent fallback |
| `src/zenith/parser.ts` | Cheerio `table tr` kv map, per-field regex on values, `ParseFailure` field-named on missing, `requireKv` fallback keys, empty HTML → table check | PASS — traces to `zenith_bank_email_format.md` |
| `src/gmail/watch.ts` | Topic not hardcoded `projects/${GOOGLE_CLOUD_PROJECT}/topics/${GOOGLE_PUBSUB_TOPIC}`, `registerWatch` persists historyId+expiration, 401 invalid_grant → `sendOnce` 1h, `scheduleWatchRenewal` 24h unref | PASS |
| `src/gmail/push-handler.ts` | Missing data →400, invalid json→400, `lastHistoryId` stored vs `newHistoryId`, `fetchHistorySince`, cursor-after-success, 404→poll fallback + 200 ACK, 500 keeps cursor | PASS |
| `src/gmail/poll.ts` | `buildQuery` `(from:domains OR …) after:UNIX`, `pollRunning` guard, `maxResults 50` paginate, per-page `setPollAfterMs(Date.now())`, 429 backoff, `schedulePollSweep` unref default 15 | PASS |
| `src/db/pool.ts` | `ssl:{rejectUnauthorized:false}`, `max:10 idle 30000`, `pool.on('error')` idle crash guard, `_setPoolForTests` mock injection | PASS |
| `src/observability/staleness.ts` | `formatInTimeZone Africa/Lagos HH:mm`, `[07:00,21:00]` inclusive, no weekend guard, 60m threshold, double cooldown, `never_seeded`/`outside_hours`/`ok`/`stale`, 60s `startStalenessChecker` unref+isRunning | PASS |
| `src/alerts/alerter.ts` | `sendOnce` Map cooldown, `redactedHost` never logs token, Telegram `api.telegram.org` with `chat_id`+`parse_mode HTML` vs generic `{text,key}`, fallback `ALERT_FALLBACK_WEBHOOK_URL` on throw/non-2xx, distinct `Possible spoof` vs `Zenith format drift` vs `Pipeline stale` | PASS |
| `src/worker.ts` | `processEmail` threads `createChildLogger({email_message_id})` every `stage: received|auth|parse|validation|dedup|insert`, alerter wiring suspicious+parseFailure, `getMessageFull` 429, `createHttpServer` POST `/gmail/pubsub` + GET `/health`, `start()` env→pool→migrate→gmail→http→watch→poll→staleness, SIGTERM drains all timers/http/pool | PASS |

---

## Additional Checks

- **Validation & sender:** `validation.ts` amount `/^[\d,]+\.\d{2}$/` comma-strip→number `>0` max 2 decimals, currency enum NGN/USD/EUR/GBP uppercased, reference non-empty, date `DD/MM/YYYY` via `date-fns parse+isValid` +5min future, `capRawEmail` 100KB byteLength strip `data:image`/`Content-Type:image/` + suffix. `sender.ts` CIP_CR/NIP/UP_IB/CHARGE + UNKNOWN lenient-safe (D-10) — all PASS via `sender.test.ts` 13, `validation.test.ts` 18, `classifier.test.ts` 16 (credit-only `CREDIT TRANSACTION NOTIFICATION` `includes` handles Fwd:/Re:).
- **Migrations:** 001 transactions (UNIQUE + CHECK amount>0), 002 suspicious_emails (UNIQUE), 003 pipeline_health seed, 004 ALTER value TEXT for history cursors — all present and idempotent.
- **Anti-patterns:** No `TBD/FIXME/XXX/placeholder/console.log` in src; no hardcoded empty data; no fetch without await. `pool.ts` ssl correct via object not query string.
- **Behavioral spot-check:** `npm test 134/134` exercised above; push 404 fallback, duplicate ROLLBACK, staleness cooldown, and suspicious vs drift prefixes all exercised via unit+integration mocks without live Gmail/Postgres.
- **Railway wiring:** `railway.json` `npm run migrate && npm start`, `Dockerfile` node:20-slim, `src/config/env.ts` zod env validation, secrets via env per NFR-1.2.

---

## Verdict

**PASS — Phase 01 achieved.** All 7 exit criteria are proven by code + 134 green tests. The 4 plan summaries accurately describe the delivered artifacts; deviations (004 TEXT migration, alerter fallback, pool retry, watch unref) were necessary correctness fixes with no scope creep.

### Guidance

- Mark `01-zenith-ingestion` **complete** in roadmap.
- Remediation: none required for phase goal. Manual staging (01-VALIDATION.md 10.7 checklist) remains recommended before production: forward real Zenith credit to connected Gmail, verify `transactions` row + `email_auth_result` DKIM `zenithbank.com`, craft spoof via test SMTP → `suspicious_emails` + Telegram alert, kill worker mid-processing → no gaps/duplicates, lower `STALENESS_THRESHOLD_MINUTES` → staleness fires. Configure Railway env: `DATABASE_URL`, `GOOGLE_CLIENT_ID/SECRET/REFRESH_TOKEN`, `GOOGLE_CLOUD_PROJECT/TOPIC`, `ZENITH_SENDER_DOMAINS`, `ALERT_WEBHOOK_URL`/`TELEGRAM_CHAT_ID`/`ALERT_FALLBACK_WEBHOOK_URL`, `BUSINESS_HOURS_*` defaults already correct.
- Next: proceed to Phase 02 (WhatsApp) which consumes `transactions` as source of truth.
